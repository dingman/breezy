# PREREG v1-KALSHI — current_rung_hold sibling family on Kalshi (DRAFT)

Status: **DRAFT, not yet registered.** Registers the sibling family ordered by the strategy
lead's binding ruling, `docs/evidence/grok_prereg_v2_ratification_2026-09-04.md` §Final ruling
(2) "Kalshi sibling family prereg": *"Own PREREG, own n, own D0 = first UTC day after committed
sibling spec **and** a Kalshi-only tally running v2 boundaries. Never pool with PM
(`KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md:7-11`; `mb_current_rung_edge_2026-09-02.md:244`). Shadow
diagnostic-only (plan:21)."* This document is the family's PREREG artefact; it is not itself an
enablement — the two operator-reserved caps and Kalshi account/KYC/API-key issuance
(`KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md` S11) stay entirely outside it.

## 0. Provenance pins (frozen at registration — placeholders below until §Registration checklist closes)

| Pin | Value | Source |
|---|---|---|
| Family id | `current_rung_hold` / Kalshi (sibling, never pooled with PM) | ratification §Final ruling (2); plan:9-11 |
| Trial-id prefix | `kalshi:current_rung_hold/trial/{station}/{climate_day}` — distinct from PM's prefix so `assert_live_only` can enforce a venue-family assertion | plan §Tally F2, `:97,135`; v1 §8 pattern (trial_id) |
| Archive table pin | `archive_table_rev2` (own module, own `CORPUS_SHA256`/`STUDY_GIT_SHA`; v1's `archive_table.py` untouched) | plan §Stations "Corpus pin versioning"; addendum item 9 |
| Corpus window | 2021-01-01..2025-12-31, `mb_current_rung_edge_study.py:20,30` | plan:36 |
| n_min per cell | 90 | plan:36 ("n_min=90"), ratification (2) "Cells" |
| Parser fix (rev2 only) | `parse_metar_t_group` in `scripts/analysis/settlement_alignment_study.py` must accept the 4-digit short T-group (`\bT[01]\d{3}\b`), not only the 8-digit form — measured effect includes KBOS 7.1→59.4, KDEN 62.6→64.1, KEWR 49.4→65.7 rows/day; fixed RED-first, **only in the rev2 pinned table**, v1's `archive_table.py`/`CORPUS_SHA256` stay byte-identical | addendum item 9; `kalshi_station_temp_cadence_2026-09-04.md:35-39` |
| Boundary table | Same v2 boundary artefact (§5 below), generated by `scripts/analysis/crh_group_sequential_boundaries.py`, sha256 of the input parameter set | v2 draft §7 |
| Sequential-tally path | New sibling module (e.g. `scripts/analysis/family_tally_v2.py`) consuming `ScoredTrial` directly — **v1's `live_family_tally.py`/`mb_current_rung_edge_study.build_realized_stratum` stay byte-unmodified while v1 is live**, pinned by an AST/import-path non-modification test | addendum item 3 (converged S2 scoping) |

## 1. Hypothesis (falsifiable, one sentence)

Among live 1-contract IOC buys of the Kalshi venue rung currently containing the unambiguous
running max, on the family's own admitted station set, taken only when the frozen archive
Wilson-lower `p_hold` (rev2 cells) exceeds the per-trial `BE_i = entry_ask_i + fee_i`, the
**realized** hold rate crosses the v2 group-sequential efficacy boundary against
`π_k = mean_i(BE_i)` before the futility boundary or the calendar/loss stop — mirroring v1 §1's
falsification shape but under v2's sequential test, never v1's fixed 60/150 Wilson comparison
(v1 §1:24-28; ratification (1)(a); v2 draft §3).

## 2. Population

- **Stations — admission gate is ordered and none of the 19 candidates is admitted by this
  document alone** (`kalshi_station_cadence_2026-09-04.md:139` "No station is admitted on this
  file alone"):
  1. **Cadence, two sample months including winter**, rule = median-of-daily-medians ≤5 min
     **and** ≥90% of days with daily median ≤5 min (the table's actual governing rule, not the
     header's stray 95% figure — `kalshi_station_cadence_2026-09-04.md:9`; ratification (2)
     admission gate item 1). **Temperature-bearing** cadence on **both** feeds (live
     `api.weather.gov` observations and archive IEM METAR T-group) is additionally required, per
     `kalshi_station_temp_cadence_2026-09-04.md:9`.
  2. Station name taken from `rules_primary` only, never `settlement_sources` or geography
     (`KALSHI_DAILY_TEMP_SERIES_ENUMERATION_2026-09-04.md:12,46`; ratification (2) item 2).
  3. **CLI-final vs venue-settled**, per station, n≥90 settled HIGH events, agreement = venue
     `result` equals the CLI-FINAL winner via `WeatherBucketFacts.contains`, Wilson lower
     (`z=1.959963984540054`) > 0.99, voids counted separately and **raise** — the PM four-station
     divergence rate is explicitly **not inherited**, it is UNMEASURED for Kalshi
     (ratification (2) item 3; `KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md` S15).
- **Candidate set (19), current status:**
  - **18 PASS on both feeds, both sample months:** KATL, KAUS, KBOS, KDFW, KHOU-cadence-only
    (see below), KLAS, KSDF, KMSP, KMSY, KEWR, KOKC, KPHL, KPHX, KSAT, KSAN, KSEA, KTTN, KDCA
    (`kalshi_station_cadence_2026-09-04.md:80-100`; `kalshi_station_temp_cadence_2026-09-04.md:11-32`).
  - **KHOU — HELD, gate item 1 incomplete.** Cadence PASS in Jan/Oct 2025 (July's 83.3% was a
    MADIS archive outage, not slow cadence); but its live 5-minute feed itself is clean while its
    **archive** feed carries a coverage gap mid-2025-07 (5-min rows stop 07-04 on, hourly-only
    after) — usable 2021–2025 corpus density must be sized in S9 before admission
    (`kalshi_station_temp_cadence_2026-09-04.md:18,43-44`; plan:64).
  - **KDEN — HELD, gate item 1 fails on the live feed.** Archive cadence is clean (5.0 min, 100%
    both sample months) but the **live** NWS feed on 2026-09-03 published only 6 observations in
    the window (60-min median) — live 5-minute publication for KDEN appears to have stopped; must
    be rechecked on ≥2 further days before admission or exclusion
    (`kalshi_station_temp_cadence_2026-09-04.md:17,46-48`; addendum item 8).
  - **NYC/KNYC excluded**, same reason as v1 — hourly-only on both feeds, confirmed as a control
    (`kalshi_station_temp_cadence_2026-09-04.md:33,50`; v1 §2:32-34).
  - **Gate item 3 (CLI-final vs venue-settled) is UNMEASURED for every candidate as of this
    draft** — no candidate clears the full ordered gate yet. The frozen admitted set is fixed
    **only at registration**; any station added after registration is a **new family**, mirroring
    v1 §7's restart rule (v1 §7:147-150).
  - Existing four PM cities (LAX, MDW, MIA, SFO) may also run on Kalshi — **same-city fills on
    both venues are admissible in each family** (pseudo-replication is within-family only,
    ratification (2) "Same-city both venues"), but they add PnL rows, **not independent hold-rate
    events**, so they do not accelerate this family's own clock beyond its own `n`
    (`KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md:9-11`).
- **Legal cells, unit of observation, and "not trials" (venue skip-days, archive-vs-live
  pooling)** — unchanged from v1 §2 (`decision.py:29-40,214-225`; v1 §2:38-47); this family reuses
  `config.py`/`decision.py` unmodified in structure, only its instance parameters differ (§3).
- **Shadow rows are a diagnostic prefix only.** Unfilled Kalshi shadow candidates measure take
  rate and selector reachability; they enter **neither** the kill nor the survive side, never
  enter the sequential tally, and captured days are never retroactively scored
  (`KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md:21`, R2).

## 3. The exact take rule (frozen per instance) — selector unchanged, θ substituted

Evaluated by the same `evaluate_decision` (`decision.py:238-287`) in the same binding order as v1
§3 — no rule-order change for this family. Per-instance parameter substitutions:

- `stations=` the admitted subset only (frozen at registration; see §2).
- `required_fee_coefficient=Decimal("0.07")` — **UNVERIFIED**
  (`KALSHI_INTEGRATION_PLAN_2026-09-03.md:77` §5.2). Form is VERIFIED:
  `fee_type="quadratic"`, `fee_multiplier=1` on all 102 daily series
  (`KALSHI_DAILY_TEMP_SERIES_ENUMERATION_2026-09-04.md:11`), so the *functional form* Breezy's
  `_fee = theta*ask*(1-ask)` shape targets is confirmed; only θ's numeric value is not.
- `fee_rounding="up"` (a new `CurrentRungHoldConfig` field, `{"half_even","up"}`, defaulting
  `"half_even"` for v1; Kalshi pins `"up"`, pessimistic-only) — **UNVERIFIED**
  (`KALSHI_INTEGRATION_PLAN_2026-09-03.md:79` §5.4).
- `archive_table_pin=archive_table_rev2.CORPUS_SHA256` (own module; v1's pin untouched).
- All other frozen defaults (`executable_ask_lower/upper`, `minimum_displayed_size`,
  `order_quantity=1`, `allow_short=False`, `entry_only_halt=True`, staleness rule) inherit v1 §3
  unchanged (`config.py:179-189`).
- **This family runs SHADOW until both of the following hold, per instance:** (a) θ and rounding
  are pinned against Kalshi's *published* fee schedule, and (b) at least one real Kalshi fill
  confirms the schedule in practice (ratification (2) "Selector/scoring"; plan §Fees item 1).
- **Settlement-leg fee is MISSING from the model** (`KALSHI_INTEGRATION_PLAN_2026-09-03.md` §5.5;
  `trial_scorer.py:187` assumes a zero settlement leg). It must be measured on the first
  held-to-settlement contract and, **if non-zero, added to `FilledTrial` before any Kalshi row
  enters a verdict** — a hard precondition, not an optional refinement (plan:86).

## 4. The exact scoring rule (frozen)

Identical to v1 §4 (`SCORER_TALLY_BCA_BRIEF_2026-09-04.md`) with two additions carried from the
converged S2 scoping (addendum item 3, ratification (1)(e)):

- **`venue` field on `FilledTrial`/`ScoredTrial`**, plus fee-source; the live trial-id prefix
  encodes the venue; a `venue:` stratum exists so a `cell_dead` kill can fire on Kalshi alone.
  **No pooled path, no pooled flag** (`KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md` §Tally F2).
- **Per-trial break-even applies to this family's tally only.** `BE_i = entry_ask_i + fee_i` (fee
  already stored on `ScoredTrial`, `trial_scorer.py:107,138`); the module-constant
  `break_even(mean_ask)` used by v1's live path is **not** used here. This is the θ-parameterised,
  concave-fee-correct form the strategy lead ruled in for v2/Kalshi specifically — **v1's own
  tally stays on `break_even(mean_ask)`, byte-unmodified** (ratification (2) "S2 per-trial BE:
  v2/Kalshi tally path only; v1 byte-identical"; addendum item 3).
- FINAL-print-only scoring, the fallback-exclusion rule, and the ask-band/basis split are
  otherwise unchanged from v1 §4.

## 5. The exact pre-declared analysis (frozen) — v2 group-sequential rule, inherited verbatim

This family adopts v2's sequential design in full, not a Kalshi-specific variant
(ratification (2) "Kalshi's own PREREG is drafted against v2's group-sequential design, not v1's
floors," `KALSHI_CRH_EXPANSION_PLAN_2026-09-04.md` addendum item 5):

- **Statistic:** `Z_k = (p̂_k − π_k) / sqrt(π_k(1−π_k)/n_k)`, the Wilson score form (not Wald —
  the strategy lead's explicit correction of the draft's mislabel), with `p̂_k = k/n_k` on filled
  Takes only and **`π_k = mean_i(BE_i)`** per this family's own §4 per-trial BE
  (ratification §Final ruling (1)(a); v2 draft §3:39-43).
- **Boundary form:** Lan-DeMets O'Brien-Fleming-type alpha-spending, two one-sided tests, each
  **α = 0.025** (matching v1's two-sided-95%/one-sided-97.5% `z = 1.959963984540054`, so no Type I
  inflation relative to v1 — v2 draft §2:21-25, ratification (1)(a)).
- **Look schedule:** every 10 filled Takes, `n_k = 10, 20, …, 160`; **n_max = 160** (smallest
  multiple of 10 ≥ the strategy-lead floor of 154; preserves v1's n=150 SURVIVE floor as look 15 —
  v2 draft §3:54-61, ratification (1)(b)).
- **Information fraction:** `t_k = n_k / n_max` — **not** calendar time (ratification (1)(c),
  rejecting the draft's open question 2).
- **Sequential monitoring is pooled-only**; station and ask-band strata stay fixed-rule
  `cell_dead` kills at n≥60, using this family's own `mean(BE_i)` in those strata too
  (ratification (1)(d)).
- **Terminal quantities unchanged and terminal-only:** the BCa ROI bound
  (`src/breezy/settlement/roi_bound.py:93-112`) is computed once, at the final decision, never
  per-look; ΣPnL > 0 is required in addition to any boundary crossing for SURVIVE (v2 draft §5).
- **Boundary table generation is a prerequisite, shared with v2** — `n_max=160`, α=0.025,
  Lan-DeMets OBF, per-look `BE_k` from this family's own admitted-station pooled mean —
  `scripts/analysis/crh_group_sequential_boundaries.py` (v2 draft §7); this document does not
  duplicate that script, it pins the same one, run against this family's own data.

## 6. Stop rules

- **KILL** → stop immediately, no resurrection — identical trigger structure to v1/v2
  (efficacy/futility boundary crossing, or any n≥60 stratum dead).
- **Time stop:** **D0 + 165 calendar days** — same clock shape as v1/v2 (v1 §6:121-123;
  v2 draft §4). Family-specific D0 per §7 below.
- **Loss stop:** cumulative **ΣPnL ≤ −60 contract-units** (contract units, never dollars — dollar
  caps are operator-reserved; v1 §6:124-128, inherited by v2 draft §4).
- **Spending-to-horizon at truncation:** if either stop fires before `n_max = 160`, the boundary
  generator emits a final analysis at the truncation look using cumulative alpha spent through
  that look (observed information fraction), never an unadjusted final look (v2 draft §4:76-82).
- **Pending trials at horizon:** unchanged from v1 §6:132-135 — excluded from `n` and from the
  stop-rule ΣPnL until scored or fallback-stamped; no new trial after D0+165.
- **Structural-dead pin**, identical machinery to v2 §9's amendment target
  (ratification (1)(f)), with the **Kalshi family's admitted station set as the denominator**:

| Piece | Definition | Cite |
|---|---|---|
| Window | [12:00,17:00) LST | v1 §2:35-36 |
| Afternoon-covered | span of distinct captured quote instants in that window ≥30 min; 0–1 instant ⇒ 0 | `ma_prelock_winner_ask_study.py:160,323-332,360-361` |
| Listed | venue listed that station-day; skip-days ∉ denominator | v1 §2:45-47 |
| Denominator | covered **listed** station-days of **this family's admitted Kalshi station set** (not PM's four) | ratification (1)(f); this doc §2 |
| Fire at | count ≥15 | `MIN_AFTERNOON_STATION_DAYS=15` |
| "≈0" | filled Takes = 0 on those days; one fill kills this stop (L-9 "no seller") — not a Wilson take-rate test | ratification (1)(f) |

## 7. D0 and clock (arithmetic, not a forecast)

- **D0 rule (binding, verbatim from the strategy-lead ruling):** the first UTC day **after** (a)
  this document is committed as the family's spec, **and** (b) a Kalshi-only tally is actually
  running against v2's boundary table for its verdict logic (mirroring v2 draft §8's own D0 rule,
  restated for this family; ratification §Final ruling and (2)). Any fill occurring before both
  (a) and (b) does not belong to this family.
- **Clock arithmetic**, stated as arithmetic under named assumptions, not a commitment: 18
  July-PASS candidates + 4 dense PM stations = 22 stations (KDEN/KHOU HELD, excluded from this
  arithmetic pending §2's gate); observed take rate 0.25 taken/listed-station-day
  (`venue_city_census_2026-09-03.md:45`; the archive-implied ~0.75/day is deliberately not used).
  Events/day = 22 × 0.25 = 5.5. `n=60` at ≈ D0+11 d; `n=150` at ≈ D0+28 d; `n=160` at ≈ D0+30 d
  (ratification (2) "Clock"). **Kalshi's actual take rate is UNVERIFIED and must be re-sized from
  shadow (S10) before this arithmetic is treated as a schedule.**

## 8. What will NOT change after the first Kalshi order

Frozen for the life of this family, mirroring v1 §7's shape: every per-instance value in §3; the
rev2 archive table's cells, `CORPUS_SHA256`, `STUDY_GIT_SHA`; the take-rule order and refusal-reason
set (unchanged from `decision.py`); the per-trial BE formula and rounding mode for this family's own
tally; the v2 boundary parameters (α=0.025, OBF spending, look schedule, n_max=160); the 60/150-as-
look-15 relationship; the BCa statistic, B, seed, and 0.20 exclusion ceiling; the admitted station
set. No floor is lowered, no post-hoc screen is added. **Any change requires a NEW pre-registration
document; this family restarts (n resets to 0) rather than being amended in place** — v1 §7's rule,
inherited unchanged (v1 §7:147-150). **This family never pools with the PM family, at any point, for
any reason** — that constraint alone is never subject to the "new spec" override; a merge would be a
different hypothesis, not an amendment.

## 9. Registration checklist — must ALL be TRUE before DRAFT → BINDING

| # | Item | Not yet true because |
|---|---|---|
| 1 | **θ verified.** `required_fee_coefficient` confirmed against Kalshi's published fee schedule (reconciled with the confirmed `quadratic`/`multiplier=1` form) | currently `Decimal("0.07")` UNVERIFIED (`KALSHI_INTEGRATION_PLAN_2026-09-03.md:77`) |
| 2 | **`fee_rounding="up"` verified**, and settlement-leg fee measured (zero or added to `FilledTrial`) | rounding UNVERIFIED (`:79`); settlement-leg fee MISSING entirely (§5.5) |
| 3 | **First real fill confirms (1) and (2) in practice**, not just on the published schedule | no Kalshi execution client exists yet (plan §Inventory: "Absent: `src/breezy/adapters/kalshi/`") |
| 4 | **Rev2 archive cells generated with a recorded sha256**, `archive_table_rev2.CORPUS_SHA256` pinned in §0, parser fix for the 4-digit T-group landed and re-measured | rev2 table not yet emitted (plan S9 not started) |
| 5 | **Admission evidence closed per station**, in order: cadence (2 months + temp-bearing both feeds), `rules_primary`-sourced station identity, CLI-final-vs-venue-settled (n≥90, Wilson lower >0.99, voids raising) | gate item 3 UNMEASURED for all candidates; KDEN/KHOU HELD on gate item 1 |
| 6 | **Kalshi permit and enablement are operator-gated and separate from Polymarket's** — `KalshiOrderSubmissionPermit` exists, checks its own verified-canonical-string flag and its own operator-requested flag, and cross-venue permit misuse raises `TypeError` | `OrderSubmissionPermit` is currently venue-blind and would inherit PM's already-`True` verified flag (`order_enablement.py:44,48,203`; plan S13, "Highest-severity security finding") |
| 7 | **Kalshi-only tally is running against the pinned v2 boundary table** (D0 rule clause (b)) | boundary table is still a placeholder pending `crh_group_sequential_boundaries.py` (v2 draft §3 table, §7); no `family_tally_v2.py`-shaped module exists yet (addendum item 3) |
| 8 | **Operator prerequisites satisfied** — Kalshi account, KYC/eligibility, funding, API key issuance | operator-only, on the critical path (plan S11); untouched by this document |

Until all eight are true, this document remains DRAFT and authorizes nothing — no Kalshi order,
shadow or otherwise, may be constructed against it.

## Coordinator addendum — S0 corrections this draft must absorb before registration (2026-09-04)

`docs/evidence/venue/kalshi/s0_2026-09-04/S0_FINDINGS_2026-09-04.md` (public API + vendor docs, verbatim quotes) landed after this draft:
1. **Settlement source.** Kalshi's current `KX*` daily series settle "according to The Weather Company" (`settlement_sources`, `rules_primary`, `rules_secondary`), while naming the NWS CLI station. §2 gate item 3 (CLI-final vs venue-settled, n≥90, Wilson-lower >0.99) is therefore not a formality but the load-bearing admission test; the "same NWS CLI event" wording in §1/§2 must read "same weather event, different settlement source".
2. **Fee rounding.** Vendor rule is `trade_fee = ceil_6dp(model_fee)` plus a per-ORDER fee accumulator with rounding fees/rebates — not round-up-to-cent per trade. §3's `fee_rounding="up"` must become the documented accumulator, pessimistic where ambiguous; the coefficient θ remains UNVERIFIED (published only in a PDF that refuses non-browser clients; also derivable from the first fill's `average_fee_paid`). Settlement fee: "zero for simple yes/no determinations" (VERIFIED) — §3's settlement-leg clause is satisfied by that quote plus one measured settlement.
3. **Market shape.** There is no NO ticker: one ticker, `bid ≡ buy YES`, `ask ≡ sell YES`, one signed position; prices are fixed-point dollar strings on a per-market `price_ranges[].step` grid (sub-cent grids exist; `tick_size` deprecated); minimum granularity 0.01 contracts; orders via `POST /portfolio/events/orders` with `side ∈ {bid, ask}`, `time_in_force ∈ {fill_or_kill, good_till_canceled, immediate_or_cancel}`, `client_order_id`, mandatory `self_trade_prevention_type`. §3 "order_quantity=1" stands; the symbology/instrument test in the plan (F5) is rewritten to "one instrument per market, BUY-YES-only, quantity precision from `price_level_structure`".
