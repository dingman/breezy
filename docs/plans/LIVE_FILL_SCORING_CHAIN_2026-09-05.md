# Live fill -> scored-trial chain (2026-09-05) -- **Rev 8**

Plan artifact only. v1 `trial_scorer.score_trial`, the 17-column `ScoredTrial`/store and `live_family_tally.py` SOURCE are BINDING and untouched; the v1 WRAPPER `deploy/systemd/live-tally-run.sh`
may gain flags -- a wrapper change, not a v1 module change. Rev 8 applies round-7 peer review (dispositions, section 6) to sections 3.0, I2, I3, 5 and 6 ONLY; **I1a/I1b/I3c/I5 are UNTOUCHED** (a builder is implementing I1b)
and **BLOCK-2 is CLOSED** -- Rev 5's marker wording (ONE writer, the wrapper) is kept verbatim everywhere it appears. Rev 3/4 dispositions A1-A8/B1-B3/R1-R9/N3, Rev 6's BLOCK-1.1/1.2/1.3 + Pins 1-5, and Rev 7's BLOCK-a/BLOCK-b/REVISE-1/REVISE-2/NOTE-3 stand as amended by section 6 below. Citations re-verified 2026-09-05 against the working tree; what could not be is marked UNVERIFIED.

## 1. Goal state and the walks

**Goal (falsifiable).** For every real Polymarket.us fill from `CurrentRungHoldStrategy` on the live node:
1. a durable fill record exists in the exec `SqliteStateStore` **before any other consequence of the fill**, carrying venue_order_id, client_order_id, instrument_id, ts_event, ORDER-level
  cumulative qty/cost and the **venue-reported** cumulative fee plus its reconciliation flag (station / climate_day / entry_ask arrive by join -- I2);
2. once that station-day's NWS CLI is FINAL, a scored row for `current_rung_hold/trial/{station}/{climate_day}` exists in the scored-trials store (plus `provenance.json` + `fill_order.jsonl`)
  **at the FIRST 14:15 UTC run after the FINAL lands** -- a lag of <= 1 day when the FINAL issues after 14:15 UTC on D+1 -- ahead of that run day's 14:30 v1 and 15:30 v2 tallies; no manual step,
  idempotent, q != 1 excluded before scoring (ruling Q1, `score_live_trials.py:238-252`). The scorer is idempotent and the tallies cumulative, so the lag costs only latency. *Measured
  (`docs/evidence/nws_cli_final_issuance_vs_1415utc_2026-09-05.md`, product-header `issuance_time_ns`, 2026-08-16..09-03): every station's FINAL lands 5.3-8.0 h
  BEFORE 14:15 UTC D+1 (medians NYC -7.8 h, MDW -7.7 h, LAX -5.9 h, MIA -5.8 h, SFO -5.7 h); 90/90 routine finals early, the single late one a go-live backfill correction.*
3. the v1 structural-dead stop is actually EVALUATED over the CORRECT window: `live-tally-run.sh` passes BOTH `--fill-source` and `--covered-listed-station-days` (`live_family_tally.py:454-465`)
  with numerator and denominator sharing ONE window start -- else it silently never fires (O1) or fires falsely (A3).

**Happy walk (fill 20:31 UTC, day D).** (1) `_evaluate` latches `current_rung_hold/trial/{station}/{D}` at DECISION time with `instrument_id` + `ask` (`trial_day_latch.py:113-114`;
`strategy.py:438`); that key's `station` segment is the registry **city** key (`strategy.py:166-171`), NOT by definition the CLI location -- see I2. (2) `_maybe_submit` -> `_submit_order`
(`exec/client.py:1511`) -> `post_order`; `classify_create_order_outcome` -> `KIND_ACCEPT_FILL` (`submit_chain.py:478-493`), which **[I1a]** now also carries order-level
`cumulative_qty`/`cumulative_cost`/`cumulative_fee` and `fee_reconciled`. (3) **[I1b]** `record_fill` is the FIRST action of that branch (`client.py:1585-1613`), before `true_up_booking` (:1591),
`_retire` (:1594), `_generate_submitted` (:1595), `generate_order_filled` (:1597); it writes `exec/polymarket_us/fill/{venue_order_id}` + index (:1315-1340) and `SqliteStateStore.set` COMMITs
before returning (`runtime/sqlite_store.py:123-124,168`), so evidence lands before any other effect of the fill. (4) `generate_order_filled` publishes natively; `breezy-nws-ingest.service` lands
the FINAL CLI when NWS issues it. (5) **[I3]** 14:15 UTC, `breezy-score-live-trials.service` runs the WRAPPER `score-live-trials-run.sh`, which loops the manifest's stations and invokes `score_live_trials.py` once per city;
the scorer resolves the exec state-DB path IN-PROCESS from `POLYMARKET_US_EXEC_STATE_DB` when `--fill-source` is omitted (R8/BLOCK-1) and **never writes the marker** (BLOCK-2); **[I2]** the reader pairs each
fill to its latch by `instrument_id` -> `trial_id`/`station`/`climate_day` from the latch KEY and `entry_ask` from `TrialDayRecord.ask`; `_admit_fill` gates q != 1, `fill_below_ask` and
`fee_unverified`; `score_trial` scores against `read_climate_day_including_corrections`; `write_scored_trials` appends `score_seq`; sidecars written; exclusions appended. (6) 14:30
`live-tally-run.sh` (both stop flags + the shared window start), 15:30 `family-tally-v2-run.sh pm_us_crh_v2`; both assert the WRAPPER's dated success marker first (I3, BLOCK-2).

**Failure walks.**
- *Dies between POST and response.* `except` at `client.py:1572-1577` -> `_refuse(AMBIGUOUS_REASON)`, intent stays OPEN, no record, nothing scored. I1b runs only on a classified
  `KIND_ACCEPT_FILL`, so it changes nothing here.
- *`record_fill` raises on a real fill.* Exact control flow in I1b: ERROR + the full serialized record (the seven fields at `client.py:413-419` plus I1b's two are ids, a side, Decimals, a bool
  and a ts -- no secret, verified), `_refuse` latching `_trading_refusals` so every later submit is denied (`_submit_order` :1515-1517) and `degrade()` (:1766-1767) raises exactly one CRITICAL
  alert, the intent left OPEN (`probe_open_intent` :297; `reconcile_at_startup` `submit_intent.py:397-433` leaves it OPEN because the fill probe is a constant-`False` stub, `client.py:695-698`),
  and `OrderSubmitted` + `OrderFilled` still published so Nautilus books the position.
- *Fee cannot be reconciled to the legs.* Record written in full with `fee_reconciled=False`; `_refuse` (second fixed reason) halts later submits; retire/publish proceed -- **the fill is real,
  only the fee is untrusted**. I2 then excludes it as `fee_unverified`, so it is never scored.
- *Venue returns qty 0.37, or below the entry ask.* Recorded in full by I1b (the record is evidence, not a filter); `_admit_fill` returns `FillExclusion("partial_fill")` / `fill_below_ask`, so it
  never reaches `score_trial`, never enters Wilson n/k or sum(PnL), and lands in `excluded_fills.jsonl` and the v2 coverage table (I3c) -- loud, never silently dropped.
- *Fill source unreadable.* `_open_readonly` returns `None` for an absent or `mode=ro`-refusing DB (`fill_time_count.py:84-98`; a WAL DB whose `-shm` is missing after an unclean exit can fail to
  open read-only). The scorer exits NON-ZERO, so its wrapper writes NO dated marker and BOTH tally wrappers fail loudly: a silent zero-fill run must be indistinguishable from nothing, never from a real
  no-fill day (A5).
- *CLI never FINAL.* `score_trial` refuses `no_record`/`preliminary_only`. The venue fallback needs `venue_settlement_tmax_f`, which this source can never supply (O3), so the trial stays refused
  indefinitely and loudly -- never silently scored, never silently dropped.
- *One of four cities fails, the rest succeed.* The wrapper exits non-zero and writes NO marker, so neither tally runs on a partial store -- a per-city marker is exactly the defect BLOCK-2 forbids.
- *Scorer runs twice.* `_already_fallback_scored`/`_unchanged_since_last_score` (:376-399) skip an unchanged settlement; `_append_fill_order_entries` de-dupes on `(trial_id, score_seq)`;
  `_write_or_assert_live_provenance_sidecar` asserts, never rewrites. A real CORRECTION appends `score_seq + 1`.

## 2. Null-hypothesis verdicts (L-1; installed nautilus_trader 1.231.0)

- **Native `CacheDatabase`/`MessageBus` persistence -- RULED OUT.** `Cache.add*` forwards to a database only `if self._database is not None` (`cache/cache.pyx:1704-1708`); the kernel accepts one backing store, `"redis"` else `raise
  ValueError` (`system/kernel.py:310-329`). Breezy sets `CacheConfig(database=None)` for every node role (`runtime/node_config.py:217,499,759`) and deploys no Redis -> memory only, answers nothing after exit. Same verdict
  `fill_time_count.py:11-23` pins.
- **Native `ExecutionEngine` reconciliation -- REAL, fed nothing.** It reconciles from `generate_order_status_reports` (`live/execution_engine.py:1557`) and `generate_fill_reports` (:1191); Breezy returns `[]` from both
  (`exec/client.py:987-1013`). By-id `generate_order_status_report` (:938-985) and `generate_position_status_reports` (:1015-1055) are REAL -- the latter maps live venue positions, which is why deferring I4 loses no exposure
  visibility.
- **Native `Strategy.on_order_filled` -- REAL, deliberately NOT used.** `trading/strategy.pyx:723`, dispatched :1971. Fires AFTER publication, in the strategy process: it cannot satisfy goal (1) and cannot be read offline.
  **Verdict: the durable write belongs in the exec client (I1b); no strategy hook.**
- **Native order FSM -- SUBMITTED must precede FILLED (justifies I1b's unconditional `_generate_submitted`).** `_ORDER_STATE_TABLE` (`model/orders/base.pyx:94-157`) has NO `(INITIALIZED, FILLED)` entry; FILLED is reachable only from
  SUBMITTED/ACCEPTED/CANCELED/PENDING_UPDATE/PENDING_CANCEL/TRIGGERED/PARTIALLY_FILLED (:116,124,126,136,143,150,156), and an illegal trigger raises `InvalidStateTrigger` (`core/fsm.pyx:127`, via `Order.apply` :1029). Skipping
  `OrderSubmitted` on the failure path would make the `OrderFilled` unbookable -- the exact outcome that path exists to prevent.

## 3. Increments, in build order

### 3.0 -- Stage-0 shared contracts (frozen before the fan-out)
I2 and I3 are built in parallel, so these surfaces are frozen HERE and neither increment may redefine them:
- **(a) Scorer CLI.** source rule: `--fills <jsonl>` selects the JSONL source; otherwise the state-DB source, path from `--fill-source <path>` else the env var (R8); `--fills` together with `--fill-source` is a bad-flag combination -> non-zero ( verified `--fills` is `required=True` TODAY at `score_live_trials.py:530`, so it becomes optional-but-exactly-one-of), plus `--city <CITY>`, ONE city per invocation, plus **`--family-manifest <path>` (BLOCK-a)** -- loaded with the shipped strict `load_family_manifest` (`persistence/family_manifest.py:115`, verified signature `(path, *, allow_draft: bool = False)`; **called with NO `allow_draft`**), the SAME file the I3 wrapper hands the counter, so the census the positive control tests and the census the denominator counts are ONE registered file rather than two agreeing lists; a missing, unreadable or DRAFT manifest is a non-zero exit. **There is NO `--since-climate-day` flag (REVISE-6, round-7).** The date the scorer windows on is `manifest.d0_climate_day` (`family_manifest.py:107`; `deploy/families/pm_us_crh_v2.json:5` = `"2026-09-05"`, verified), read from the SAME file `--family-manifest` already loads -- so d0 cannot be handed to the scorer as an argument at all, and cannot disagree with the census it travels with. **`--family-manifest` is REQUIRED on BOTH sources** (`--fill-source` and `--fills`); a fixture run points it at a test manifest rather than dropping it. The counter (I3 d0 plumbing) loads the SAME registered file and echoes `fetch_start` into its `--output` JSON, with a RED pinning `fetch_start == manifest.d0_climate_day`, so the v1 stop's `--fill-since-climate-day` -- which the wrapper still `sed`s out of that JSON -- carries the same one date BY TEST rather than by convention (strategy-lead Q4, `docs/evidence/grok_admission_exclusions_ack_2026-09-05.md`: "Scorer `since_climate_day=d0` so pre-D0 live rows are never written"). An unreadable/absent fill source -> **non-zero exit, NO marker** (A5). **Exit-code contract (frozen HERE, because I2 and I3 are built in parallel against opposite sides of it): `exit 0` means the run COMPLETED -- and that INCLUDES zero fills, every trial refused, and `instrument_unavailable`. Non-zero is reserved for faults meaning the scorer COULD NOT DO ITS JOB: a bad flag combination, a config/IO error, an unreadable or absent fill source, the store positive-control failure (I2, BLOCK-1.2), a node-env pre-flight MISMATCH or DISCOVERY_FAILED (I2, REVISE-1/REVISE-4), and a missing or DRAFT manifest.** **`malformed_input` is STRUCK from that list (BLOCK-b, round-6 correction).** It is a per-ROW refusal that never aborts the batch -- verified: each occurrence appends a `ScoreRefusal(reason="malformed_input")` for that one row and scoring continues (`score_live_trials.py:39-41,198-201,230-232,289-292,465-468`), and `main()` returns 0 at `:561` however many accumulated. Rev 6 listed it as a non-zero cause, which would have made one bad row indistinguishable from a broken scorer AND suppressed the marker for the other four cities. The two-fills-on-one-latch join refusal is exactly this shape: keeping BOTH rows out of `n` IS the protection, and it is complete without also failing the run. "Nothing to score today" and "the scorer could not do its job" therefore never share an exit status -- the marker (b) asserts the former and its absence the latter, which is the whole reason the tallies may trust it.
- **(b) Success marker -- exactly ONE writer, the wrapper (BLOCK-2).** `score_live_trials_ok_$STAMP`, `STAMP=$(date -u +%Y-%m-%d)` -- the stamp both shipped wrappers already compute (`live-tally-run.sh:20` -- **line corrected in Rev 6, verified; `:21`, not `:22`, is `STATUS=0`** -- and `family-tally-v2-run.sh:70`) -- under `$BREEZY_LIVE_TALLY_OUTPUT_DIR` (default `$HOME/.local/share/breezy/derived`, `live-tally-run.sh:13`). **`score-live-trials-run.sh` writes it, and only after EVERY city's scorer invocation exited 0; the scorer python NEVER writes a marker; any non-zero exit anywhere in the loop -> no marker and a non-zero wrapper exit.** A per-city or python-side write would let a partial run satisfy both tallies.
- **(c) Excluded-fills line: exactly 8 keys** -- `trial_id, station, climate_day, venue_order_id, qty, reason, filled_at_ns, scored_run_utc`; idempotent on `(venue_order_id, reason)`. **What can carry them (BLOCK-3, round-7): the DRIVER-LOCAL `FillExclusion` (`score_live_trials.py:205-220`; today `trial_id, station, climate_day, qty, reason, detail`) gains `venue_order_id: str = ""` and `filled_at_ns: int = 0`, with `scored_run_utc` stamped at write time. `ScoreRefusal` (`trial_scorer.py:141-147`, verified) carries ONLY `trial_id, reason, detail` and is v1-BINDING inside the AST-pure `settlement` package, so it can NEVER populate this line** -- which is why every fill-level integrity event in I2 is an EXCLUSION, not a refusal. The defaults keep the ONE construction site (`score_live_trials.py:245`, inside `_admit_fill`); `test_score_live_trials.py:466,475,489` assert on it but never construct it; `kw_only=True` makes the defaulted fields order-safe -- formerly mis-stated as three shipped construction sites green (`_admit_fill:238-252`; `tests/unit/test_score_live_trials.py:466,475,489` -- the complete set, verified by impact query); the state-DB path always supplies the real `venue_order_id`, so `(venue_order_id, reason)` idempotence holds for every line the LIVE path writes.
- **(d) Record JSON names:** `cumulativeFee` / `feeReconciled` (I1a/I1b), consumed BY NAME in I2's reader and I5's fixture.
- **(e) The city census.** The wrapper loops `load_family_manifest(deploy/families/pm_us_crh_v2.json).stations` (`persistence/family_manifest.py:110,115`; the file's four stations LAX/MDW/MIA/SFO, `pm_us_crh_v2.json:8-13`) -- the SAME census the denominator counts (I3 d0 plumbing), so numerator and denominator share one station (note: `family-tally-v2-run.sh:17` already spells `"$REPO/deploy/families"`, which the byte-identity RED does not cover -- out of scope) -- set BY CONSTRUCTION, not merely by a test. **ONE manifest literal, TWO wrappers, ONE test (BLOCK-1, round-7).** The path reaches three consumers -- the scoring loop's cities, EVERY scorer invocation's `--family-manifest`, and the counter's `--family-manifest` -- but the counter is invoked by `live-tally-run.sh` and the scorer by `score-live-trials-run.sh`, so the literal is assigned in TWO wrapper files, not one. Each carries the byte-identical line `FAMILY_MANIFEST="$REPO/deploy/families/pm_us_crh_v2.json"`, repo-relative from the `REPO=/home/jon/breezy` each wrapper already hard-codes (`live-tally-run.sh:10`, `family-tally-v2-run.sh:15`) (**verified: `live-tally-run.sh:10`; the shipped sibling `family-tally-v2-run.sh:15` uses the same idiom**), and a RED in the new deploy test module asserts **the two `FAMILY_MANIFEST=` assignments are byte-identical** -- the same shape BLOCK-1.3 uses for the `Environment=` line. Each consumer loads the file itself with the strict loader, so the loop's cities, the denominator's cities and the positive control's station set cannot diverge without the committed file changing, and the counter echoes `manifest_sha256` (`family_manifest.py:112`) so such a change is visible in the artefact.
- **(f) Store dir = the scorer's EXISTING `--derived-dir`** (`score_live_trials.py:534`; default `DEFAULT_DERIVED_DIR = ~/.local/share/breezy/derived/scored_trials`, :106) -- no new flag. The wrapper passes `--derived-dir "$STORE_DIR"`, `STORE_DIR` being the `BREEZY_SCORED_TRIALS_DIR` default both tally wrappers already read (`live-tally-run.sh:12`, `family-tally-v2-run.sh:23`), and `excluded_fills.jsonl` is written IN that directory -- so I3c reads it where the v2 tally already points.
- **(g) `scored_run_utc` format:** `YYYY-MM-DDTHH:MM:SSZ` -- UTC and lexically sortable, the shape the shipped wrappers already emit (`date -u +%Y-%m-%dT%H:%M:%SZ`, `live-tally-run.sh:18`), so I3c's "latest line wins" is a plain string comparison.
- **(h) "already scored" is asked of the SHIPPED reader:** I3c calls `read_scored_trials(derived_dir)` (`persistence/scored_trial_store.py:104`, verified) and looks for any row with that `trial_id` -- the same function the scorer's own idempotence guards use (`score_live_trials.py:366-370`). Never a second parquet reader.
- **(i) Counter `--output` JSON keys, exactly six:** `{"count": int, "fetch_start": str, "fetch_end": str, "stations": [str], "depth_root_present": bool, "manifest_sha256": str}`, written `json.dump(..., indent=2, sort_keys=True)` so every key is on its own line and the wrapper can extract `count`/`fetch_start` with `sed` -- no `$PY -c`, which would collide with the shell-stub idiom below. **The two extractions are PINNED VERBATIM and anchored** (under `sort_keys=True, indent=2`, `fetch_end` PRECEDES `fetch_start`, so no positional/`head` scrape is safe): `COUNT=$(sed -nE 's/^  "count": ([0-9]+),?$/\1/p' "$CJSON")` and `D0=$(sed -nE 's/^  "fetch_start": "([0-9]{4}-[0-9]{2}-[0-9]{2})",?$/\1/p' "$CJSON")` -- two-space indent anchored to `^`, `$`-anchored, optional trailing comma, key name quoted so no other key can match. **An EMPTY or non-matching capture is a HARD failure**: the wrapper exits non-zero with one value-free line rather than passing an empty string to `--covered-listed-station-days`/`--fill-since-climate-day` (an empty `--covered-listed-station-days` would be a `None` denominator = the stop silently unevaluated, O1 again).
**I3 does not depend on I2 being built:** its wrapper tests stub the scorer with a shell `python` (the shipped `BREEZY_FAMILY_TALLY_V2_PYTHON` idiom, `tests/unit/test_family_tally_v2_deploy.py:41-47,95-99,127-133`), so argv, marker and failure propagation are pinned against a stub, never a real scorer.

### I1a -- order-level totals on the classified outcome (REQUIRED; `exec/submit_chain.py` only)
- **Which struct: `CreateOrderOutcome` (:118-128), NOT `FillGeneration` (:104-116).** `FillGeneration` is documented as "Native arguments for `generate_order_filled`" and its fields map 1:1 onto
  the native call (`client.py:1597-1612`); order-level totals are not native call arguments. `CreateOrderOutcome` already carries the order-level `venue_order_id` and `filled_cost_usd`, so the
  remaining totals belong beside them. New fields: `cumulative_qty: Decimal | None`, `cumulative_cost: Decimal | None`, `cumulative_fee: Decimal | None`, `fee_reconciled: bool`.
- **Qty and cost are sourced TOGETHER, from the order object, or together from one leg -- never mixed.** `cumulative_qty` is `_cum_quantity(payload)` (:347-364, already reading `cumQuantity` off
  `payload` or `payload["order"]`); `cumulative_cost` is `avgPx * cumQuantity` -- exactly the preference `_filled_cost_from_execution` (:383-402) already applies. If the order object lacks
  `avgPx`/`cumQuantity`, BOTH fall back to the same single execution's `lastShares` / `lastPx*lastShares` pair. Pairing a one-leg qty with a whole-order cost (or the reverse) is the defect this
  rule forbids.
- **Fee, fill-type legs only.** Filter `executions` to `type in {EXECUTION_TYPE_FILL, EXECUTION_TYPE_PARTIAL_FILL}` (`sdk_snapshot/polymarket_us_0.1.2/types/orders.py:34-43`, verified).
  `cumulative_fee` = order-level `commissionNotionalTotalCollected` when present (`types/orders.py:90`; OpenAPI snapshot `docs_snapshots/api-reference_orders_create-order_2026-08-25.md:333-335`,
  "Total notional value of all commissions collected on this order"), else the sum of per-leg fill-type `commissionNotionalCollected` (`types/orders.py:108`; OpenAPI :270-272, "in this
  execution"). A CANCELED/REJECTED/EXPIRED/NEW/REPLACE/DONE_FOR_DAY row carrying a commission is never summed.
- **`fee_reconciled`** = (sum of fill-type `lastShares` == `cumQuantity`) AND (order total absent, OR order total == the per-leg fill-type sum). The quantity identity alone detects a
  missing fill leg (a dropped leg makes the sum fall short of `cumQuantity`), so the absent-total branch needs no extra single-leg condition -- round-3 domain review: a q=1 order filled as
  0.5 + 0.5 with no echoed total is correctly costed and must be scored, not routed to `fee_unverified` (silent power loss). `Execution` is `total=False` (`types/orders.py:95-108`), so a leg with NO `type` is not fill-type: it fails the qty identity and lands `fee_reconciled=False`. Fail closed,
  never guess. `client.py` consumes these fields and never re-parses `response.body`. `_durable_execution` (:361-383) is NOT changed -- the leg chosen for `FillGeneration` keeps its selection
  rule; I1a only adds order-level totals alongside it.
- **RED tests** (existing `tests/unit/test_polymarket_us_submit_order_chain.py`): multi-leg fixture asserts qty AND cost AND fee are ORDER-level; non-fill rows carrying a commission are ignored;
  order total != leg sum -> `fee_reconciled=False`; two fill-type legs 0.5 + 0.5, no order total -> reconciled with `cumulative_fee` = the leg sum; a leg with no `type` -> not reconciled.
- **Barriers.** `submit_chain.py` is already inside `exec/` and its test module is ALREADY in X1's exact set (`test_execution_egress_firewall_guard.py: 2673-2689`), so **X1 widening for I1a is
  zero**. E0-TRANSPORT: the module stays pure (no I/O, no import beyond the already-imported `Decimal` :19). B6/B9 (`test_polymarket_us_readonly_guard.py`): no new write verb, host literal or SDK
  import.

### I1b -- call `record_fill` FIRST in the `KIND_ACCEPT_FILL` branch (REQUIRED; `exec/client.py` only)
- **Primary justification is not scoring -- the client self-denies without it.** `_map_position` (:1089) -> `_entry_price` (:1183) -> `_entry_price_from_records` (:1217): with no durable record
  for an instrument the venue still reports, `_refuse` fires (:1231-1236), and the venue-cost-basis fallback refuses too (:1200-1213); a latched refusal denies every later submit (`_submit_order`
  :1515-1517). **Verified: today, one fill followed by ANY restart before that market expires makes the client refuse to trade for the rest of the run.**
- **Interface -- exactly TWO new fields on `DurableFillRecord` (:386-419):** `cumulative_fee: Decimal` (JSON `cumulativeFee`, string) and `fee_reconciled: bool` (JSON `feeReconciled`), both taken
  from I1a's outcome -- honest by construction, never re-derived from a fee schedule (A13). No station/climate_day/entry_ask: those are durable in the latch record and recovered by joining on
  `instrument_id` (`fill_time_count.py:51-58,129-158` already ships and tests that join); copying them here would put strategy vocabulary in the venue layer and create a second truth for
  `entry_ask`. Read-time: `fill_px = cumulative_cost/cumulative_qty`, `fee = cumulative_fee/cumulative_qty` (per contract, v1 §4), `qty = cumulative_qty`, `filled_at_ns = ts_event`.
- **Exact control flow (the branch at :1585-1613, which has NO try/except today):**
  ```
  try:
      self.record_fill(record)                     # FIRST statement of the branch
  except Exception as exc:                         # noqa: BLE001 -- deliberately broad, see below
      self._log.error(f"{_FILL_WRITE_FAILED}: {type(exc).__name__}: {exc}; record={record.to_bytes().decode()}")
      self._refuse(_FILL_WRITE_FAILED)             # ONE fixed module-level reason string
  else:
      self._ledger.true_up_booking(booking, filled_cost_usd=outcome.filled_cost_usd, now_ns=now_ns)
      self._retire(intent.intent_id, retire_name, now_ns)
  self._generate_submitted(order, now_ns)          # UNCONDITIONAL
  self.generate_order_filled(...)                  # UNCONDITIONAL
  return
  ```
- **Why `except Exception` is deliberately broad:** it guards ONE evidence write, and ANY failure of it must halt trading rather than lose the event. Known raisers: `PolymarketUSError` from an
  unreadable index (:1331-1336), `sqlite3.Error` surfaced by `_store_set` (:1392), encode/serialize errors from `to_bytes` (:421-433). Narrowing to those three would let an unlisted exception
  escape into the `LiveExecutionClient` task handler, which swallows it (`exec_fault.py`) -- silently losing both the record and the refusal. Nothing is re-raised out of the branch and the event
  is never suppressed.
- **One fixed reason string, not a formatted message:** `_refuse` dedupes on the reason (:1759-1760), so a per-exception message would defeat the latch and re-`degrade()` on every occurrence. The
  exception detail goes in the ERROR log line, not in the reason.
- **On `fee_reconciled == False`:** the record is STILL written, `_refuse(_FEE_UNRECONCILED)` (second fixed reason) latches, and retire/publish proceed. The fill is real; only the fee is
  untrusted -- I2 keeps it out of scoring.
- **No alert import inside `exec/`:** `breezy.runtime.health` is forbidden there by barrier E0-TRANSPORT (`test_execution_egress_firewall_guard.py:1737,2341, 2353`; `client.py:1725-1727`); the
  `_refuse` -> `degrade()` -> `component_health_watch` -> `emit_alert` path already emits the CRITICAL alert exactly once. **Also in this file:** fix the stale `generate_order_status_reports`
  docstring (:987-999, "Breezy has also submitted no order").
- **Decode compatibility.** `from_bytes` requires every field (:435-481). Safe by construction: `record_fill` has ZERO production callers today, so no on-disk record exists. Measured
  (coordinator, read-only `mode=ro`, 2026-09-05 ~00:55 UTC, live exec store `state` table): 4 rows -- 3 `current_rung_hold/trial/*` latches, 1 `durability:probe`, **0
  `exec/polymarket_us/fill/%`**. Runbook: a one-line read-only pre-deploy re-check of that count, not code.
- **RED tests** (extend `tests/unit/test_polymarket_us_exec_client.py`): (a) ordering probe -- store write BEFORE `true_up_booking`/`_retire`/`OrderFilled`; (b) record decodes with fee + flag;
  (c) **failure path -- `record_fill` raises, no record, intent OPEN, refusal latched, component degraded, `OrderSubmitted` AND `OrderFilled` STILL published, nothing escapes**; (d)
  `fee_reconciled=False` -> record written, refusal latched, publish proceeds; (e) zero-fill and reject write no record; (f) `to_bytes`/`from_bytes` round-trip. Fixtures are stated schema-shaped
  from the SDK/OpenAPI snapshots -- NOT a recorded response.
- **Barriers.** `DurableFillRecord(` is constructed in FOUR test modules -- the COMPLETE set, verified 2026-09-05 -- and every one must gain the two new fields:
  `tests/contract/test_exec_client_reconciliation_contract.py` (:623, :728), `tests/unit/test_polymarket_us_exec_client.py` (:319, :1483), `tests/unit/test_fill_time_count.py` (:66),
  `tests/unit/test_live_family_tally_fill_source_cli.py` (:64). Both client test modules are already in X1's set, so no widening here either. Verify B6/B9 in
  `test_polymarket_us_readonly_guard.py` (intra-module call, no new import or chokepoint site).

### I2 -- state-DB fill reader in `score_live_trials.py` (REQUIRED)
- **File:** `scripts/analysis/score_live_trials.py`: add `read_filled_trials_state_db` + `--fill-source`; the JSONL reader is **augmented, not deleted** (`--fills` stays for fixtures/replay) and exactly one of the two is required.
  **Interface:** `read_filled_trials_state_db(path, *, family_prefix, city, cli_location, since_climate_day, stations: Sequence[str]) -> tuple[tuple[FilledTrial, ...], tuple[FillExclusion, ...], Mapping[str, tuple[bool, str]]]` (third member: `trial_id -> (fee_reconciled, venue_order_id)`; the driver passes BOTH into `_admit_fill(trial, fee_reconciled=..., venue_order_id=...)`, so the live path always carries the real `venue_order_id` into the artefact key -- round-8 BLOCK) -- **NO refusal member (BLOCK-3, round-7):** every fill-level integrity event this reader can see is an EXCLUSION, and the last
  member is `trial_id -> fee_reconciled`. `since_climate_day` is `manifest.d0_climate_day` (REVISE-6), passed in like `stations` -- the scorer owns the single manifest load and the reader stays a pure function of its arguments. **`stations` is `manifest.stations` (BLOCK-a), PASSED IN rather than re-loaded inside the reader**, so the reader stays a pure function of its arguments and the scorer owns the single manifest load. It **imports `_open_readonly` from `fill_time_count.py:84-98`** (or that helper is promoted to a shared module both import) -- never a second read-only-connect implementation. No flock; safe
  while the node runs. **`None` from `_open_readonly` -> the SCORER exits NON-ZERO, so its wrapper writes no marker** (A5, BLOCK-2).
- **station != city.** The latch key segment is the registry city key (`strategy.py:166-171`); the catalog needs the CLI location (`catalog.py:642-647`). I2 filters fills to the run's city and emits `registry.settlement_site(venue,
  city).cli_location` as `FilledTrial.station` (`registry/sites.py:371`, field :113). Verified: for all five PM.us sites the two strings are IDENTICAL today (`sites.toml:117-306`) -- the mandated accessor and the Kalshi-portable
  one.
- **Join, and its three integrity events -- EXCLUSIONS, never refusals (BLOCK-3, round-7).** Map `instrument_id -> (station, climate_day, ask)` from every `family_prefix` latch with `reason == "taken"`, then look up each `FILL_KEY_PREFIX` record. The reader emits NO `ScoreRefusal` at all: `ScoreRefusal` carries only `trial_id, reason, detail` (`trial_scorer.py:141-147`, v1-BINDING, AST-pure package) and therefore CANNOT populate the 8-key artefact line (3.0 (c)), whereas the driver-local `FillExclusion` can. The three events, one line per `venue_order_id` in `excluded_fills.jsonl`:
  (i) **>1 fill record joining ONE latch** -> exclude **EACH** fill with reason `duplicate_fill_for_latch`, one line per `venue_order_id`, each carrying its OWN `qty` and `filled_at_ns`; **both stay out of `n`** -- that IS the protection. Not hypothetical: `_next_score_seq` is evaluated per row inside the generator at `score_live_trials.py:496`, before `write_scored_trials`, so two rows for one `trial_id` would take the SAME `score_seq` and double-count `n`.
  (ii) **a fill with no taken latch** -> reason `no_taken_latch`, with `trial_id`/`station`/`climate_day` = `""` -- **empty strings are allowed for THIS reason only**, because without a latch there is no trial identity to report and fabricating one would be worse than an honest blank; `venue_order_id`, `qty` and `filled_at_ns` are always populated, so the line still identifies the fill uniquely and `(venue_order_id, reason)` idempotence still holds.
  (iii) **an instrument mapping to >1 latch** -> reason `ambiguous_latch`, one line per fill.
  Fail closed, never guess -- and all three are visible in the artefact and in I3c's coverage table rather than dropped. **Exit-code contract UNCHANGED: exclusions are exit 0** (3.0 (a)); "nothing admitted today" is a completed run, never a broken scorer.
- **Exclusion layering -- BOTH sources guarded by ONE gate (A6).** `fill_below_ask` and `fee_unverified` join `partial_fill`/`multi_fill` inside `_admit_fill` (:238-252), whose signature becomes `_admit_fill(trial, *,
  fee_reconciled: bool = True, tick: Decimal = _TICK)`. The keyword-with-default is forced by an invariant: `FilledTrial` (`trial_scorer.py:70-115`) is v1-BINDING in the AST-pure `settlement` package and gains NO field, so the flag
  arrives as a parameter; the JSONL fixture path (no venue fee attestation) keeps today's behaviour by defaulting to `True`. `FillExclusion`/`FillExclusionReason` are **driver-local** (:202-220), so widening that closed set old ->
  new touches no v1 module: `{"partial_fill","multi_fill"}` -> `+ {"fill_below_ask","fee_unverified","duplicate_fill_for_latch","no_taken_latch","ambiguous_latch"}` (the last three from BLOCK-3). Widened, never relaxed -- no member removed or re-spelled. `_admit_fill` additionally takes `venue_order_id: str = ""`: the state-DB path always passes the real id, the JSONL fixture path (which has no venue order id) keeps the default, so 3.0 (c)'s idempotence key is exact for every LIVE line.
- **Re-admission of a `fee_unverified` fill (strategy-lead Q2, `grok_admission_exclusions_ack_2026-09-05.md`):** only via a venue-sourced `record_fill` cumulative UPDATE for the same
  `venue_order_id` that independently satisfies both fee identities; the scorer and the operator never patch `feeReconciled`. **No such update path exists today** (`record_fill`
  writes once per `venue_order_id`) -- I4 territory, NOT built here; until then the fill stays excluded and visible in the artefact and the coverage table.
- **Post-registration amendment disclosure (R6).** Widening that closed set AFTER PREREG v1 was registered carries the SAME non-retroactivity argument as the D0 pin: **zero fills exist** (measured, 0 `exec/polymarket_us/fill/%`
  rows), so no already-admitted trial is reclassified; and all FIVE new reasons are DEFECT signatures (L-25 fill-below-ask), fee-attestation failures, or join-integrity failures (`duplicate_fill_for_latch`, `no_taken_latch`, `ambiguous_latch`) -- properties of the EXECUTION and of its evidence, never of the outcome -- i.e. the same class ruling
  Q1 already registered for `partial_fill`/`multi_fill` (`docs/evidence/grok_v2_registration_ack_2026-09-05.md` C3: excluded BEFORE `score_trial`, `dn = dk = dI = dS = 0`), and every exclusion is reported in the I3c coverage table
  rather than dropped. **Disclosed to the strategy lead for acknowledgement** before the family's first fill -- the same disclosure route the fill-order sidecar took (PREREG v2 §13:277-281).
- **State-DB path (R8/BLOCK-1): ONE contract, ONE supplier, TWO consumers.** The CONTRACT is the env var **`POLYMARKET_US_EXEC_STATE_DB`** (`EXEC_STATE_DB_ENV_VAR`, `adapters/polymarket_us/factories.py:187`, verified) -- the
  path source for BOTH the 14:15 scorer and the v1 tally wrapper. **SUPPLIER:** one `Environment=` line, byte-identical, in each of the two units, carrying the MEASURED absolute literal `/home/jon/.local/share/breezy/state/exec_polymarket_us.sqlite` (provenance and the `%h` drop: I3, BLOCK-1.1). **CONSUMERS:** (1) the scorer, when `--fill-source` is OMITTED, resolves IN-PROCESS via
  `resolve_store_path(os.environ)` -- **verified at `runtime/trade_supervisor.py:929-933`** (NOT `submit_intent.py`/`node_config.py`, where round 3 guessed it), the same function the supervisor's `main` uses at :979, raising
  `ExecStateDbNotConfiguredError` (:923-926) when the var is unset, and the scorer additionally fails loudly if the result is not a file; (2) `live-tally-run.sh` reads `STATE_DB="${POLYMARKET_US_EXEC_STATE_DB:?}"` (fail loud,
  value-free) and passes `--fill-source "$STATE_DB"` -- **the v1 python is BINDING and gains NO resolver.** One truth for the path; the value is a non-secret filesystem path, never echoed into an artefact.
- **Store positive control (BLOCK-1.2) -- an openable WRONG store must REFUSE, never score zero.** `_open_readonly` only proves a file is a readable SQLite with a `state` table; a wrong-but-openable path would yield 0 fills, a clean exit 0, a marker, and `filled_takes=0` into the v1 stop against a >= 15 denominator -- the manufactured KILL, arriving silently down the SUCCESS path. So: after opening read-only and BEFORE any join, the reader requires **>= 1 key matching `<family_prefix>{station}/` for ANY station in the `stations` sequence the scorer took from `--family-manifest`** (the plain `str.startswith` + two-segment split `fill_time_count.py:141-147` already performs; the key shape is `trial_day_latch.py:113-114`). **What is compared, exactly (BLOCK-a):** the latch key's CITY segment (`trial_day_latch.py:113-114`; the registry city key, `strategy.py:166-171`) against `manifest.stations`. For all five PM.us sites the city key and the CLI location are IDENTICAL strings today (`sites.toml:117-306` -- the same verification the `station != city` bullet above rests on), so the comparison is exact; that identity is a **coincidence of today's registry, never an invariant**, and if a future site's city key and station name diverge, THIS is the site that must map one to the other via `registry.settlement_site(venue, city).cli_location`. The RED test pins the comparison to the CITY segment so that divergence fails loudly instead of silently refusing a correct store. None -> the scorer exits **non-zero with reason `store_positive_control_failed`, and its wrapper writes NO marker**. **Per-STORE, not per-city, deliberately:** 3 latches exist today across LAX/MDW/SFO and MIA has NONE, so a per-city control would refuse MIA on a perfectly correct store -- and a store's identity, not a city's activity, is what this control tests. The resolved path is logged ONCE per invocation (a non-secret path, section 5 NOTE) so a wrong-store incident is diagnosable from the journal.
- **R8 siting -- a leaf module, not `trade_supervisor`.** `resolve_store_path` + `ExecStateDbNotConfiguredError` MOVE verbatim into `src/breezy/runtime/exec_state_db_path.py`; `trade_supervisor` imports them from there (one
  definition, never a copy), and its two resolver tests -- `test_resolve_store_path_requires_the_env_var` and `test_resolve_store_path_reads_the_configured_path` (`tests/unit/test_trade_supervisor.py:744-753`, verified) -- MOVE to a
  new `tests/unit/test_exec_state_db_path.py`. **Why -- rationale CORRECTED in Rev 6.** The leaf STILL imports `EXEC_STATE_DB_ENV_VAR` from `factories.py:187` (one truth for the NAME), and that edge is LEGAL, not debt: the importlinter layers contract puts `runtime`
  ABOVE `adapters` (`pyproject.toml:71-101`, `layers = [app, strategy, runtime, adapters, ingest, ...]`, `exhaustive = true`), so `runtime.exec_state_db_path -> adapters.polymarket_us.factories` is a DOWNWARD import needing no
  `ignore_imports` entry. What the move AVOIDS is everything ELSE `trade_supervisor` executes at module scope: `breezy.runtime.health` (`:43`, verified) plus `sqlite_store`/`submit_intent`/`trade_supervisor_core`, `fcntl` and
  `subprocess` (`:44-82`) -- the supervisor's process-control shell, which the scorer must not drag in to read one env var. Costs the scorer nothing new: I2's reader already imports `exec.client` via `fill_time_count.py:71`. **Neither the leaf nor the new test module touches X1:** its detector matches only imports of
  `breezy.adapters.polymarket_us.exec[.*]` (`tests/unit/test_execution_egress_firewall_guard.py:231-234,1152-1169`) and `factories` is not under `exec/` -- which is why `test_trade_supervisor.py`, importing that same constant at
  :27, is NOT in X1's set today.
- **Resolver duplicates (REVISE-2) -- the leaf absorbs the two RUNTIME sites; the adapter keeps its own, by LAYER.** Three sites resolve `POLYMARKET_US_EXEC_STATE_DB` today, all verified 2026-09-05: `trade_supervisor.py:929-933` (`resolve_store_path`; empty -> `ExecStateDbNotConfiguredError`, no path validation), `clear_submit_intent_cli.py:116-120` (inline `source.get(EXEC_STATE_DB_ENV_VAR, "").strip()`; empty -> one value-free line + `EXIT_REFUSED`), and `factories.py:359-369` (raises `SettingsError` on empty, on a NON-ABSOLUTE path, and on any `..` segment). **DISPOSITION:** the leaf `runtime/exec_state_db_path.py` becomes the ONE runtime resolver -- `trade_supervisor` AND `clear_submit_intent_cli` import `resolve_store_path` from it, never a copy -- and it applies the SAME two validations as `factories.py:359-369` (absolute, no `..`), so the three runtime consumers cannot drift onto a path shape the adapter would reject at node build anyway. **`factories.py` KEEPS its own copy, deliberately: `adapters` may not import `runtime`** -- the importlinter layers contract (`pyproject.toml:71-101`, `exhaustive = true`) places `runtime` ABOVE `adapters`, so that import is FORBIDDEN upward. This is a **known, LAYERED duplicate, stated rather than hidden**, and the RED below pins the two implementations to the same accept/reject set. **Behaviour change, stated not smuggled:** a relative or `..`-bearing value that today passes the supervisor and the clear-intent CLI now refuses there -- strictly EARLIER than the `SettingsError` the exec-client build already raises, and in the same direction (fail closed). ONE exception type covers all three refusal modes (`ExecStateDbNotConfiguredError`, detail naming the env var NAME and WHICH rule failed, **never the value** -- a deliberate divergence from `factories.py`'s messages, which echo the path), so `trade_supervisor.main`'s existing handler (`:978-982`) needs no new branch. **The clear-intent CLI DOES gain one -- stated, not smuggled (REVISE-7, round-7).** Verified today: `clear_submit_intent_cli.py:116-120` performs NO validation at all and refuses via `if not raw_state_db:` -> one value-free print + `EXIT_REFUSED` (`:117-119`), then `Path(raw_state_db)` (`:120`). Adopting the leaf's RAISING resolver replaces that `if` with a `try/except ExecStateDbNotConfiguredError` mapping the ONE exception to the SAME `EXIT_REFUSED` on the same stream, keeping the one-line value-free print SHAPE (`breezy-clear-submit-intent: <detail>; refused`) but carrying the exception's rule-naming detail (the VAR name and which of unset / relative / `..` failed) instead of the fixed "exec state db is unset" wording -- which would be a false statement for a relative path. **Rev 7's paired claims "no new branch" and "message unchanged" are therefore WITHDRAWN as self-contradictory**: there is exactly one new `except` clause, exit codes and streams are unchanged, and the print stays value-free. **The operator's CURRENT path cannot be refused by the new validation:** the measured live value is the absolute `/home/jon/.local/share/breezy/state/exec_polymarket_us.sqlite` (provenance: I3 BLOCK-1.1) -- no `~`, no `$HOME`, no `..` segment -- so it satisfies both new rules by inspection and this CLI keeps taking exactly the success path it takes today on the store in use. **Tests, named:** `test_resolve_store_path_requires_the_env_var` and `test_resolve_store_path_reads_the_configured_path` MOVE from `tests/unit/test_trade_supervisor.py` -- **line correction, verified: they are at `:743-751`, not Rev 6's `:744-753`** -- into the new `tests/unit/test_exec_state_db_path.py`, which gains two validation REDs (a relative path refuses; a `..` segment refuses) and a NO-VALUE sentinel on the exception text. `tests/unit/test_clear_submit_intent_cli.py` (155 lines; it references the env var only at `:11,41` and has **no** test of the unset-refusal path today) gains TWO REDs (REVISE-7): an unset var still yields `EXIT_REFUSED` with a value-free line naming the VAR -- **asserting the exit code and the value-free property, NOT that the message text is unchanged** -- and the measured absolute path above is ACCEPTED (no refusal), so the new validation provably cannot lock the operator out of the running store.
- **Node-env pre-flight (REVISE-1) -- every run bound to the node's LIVE store, value-free.** (Anchor: deliberately NOT `NODE_ARGV_ANCHOR` (`trade_supervisor_core.py:56` = `breezy-trade$`, a `pgrep -f` pattern); the leaf's `(^|/)breezy-trade$` prefix is what makes it a basename match on the joined cmdline, so that constant must not be reused.) BLOCK-1.1's measured literal and BLOCK-1.3's byte-identity test are DEPLOY-time facts; BLOCK-1.2's positive control is a store-CONTENT fact. None of them covers the residual (stated honestly in section 5): **a stale copy, or the OLD store after the operator re-paths the running NODE, keeps its latches** -- so it passes the positive control, the scorer exits 0, the marker is written, and the v1 wrapper reading the same env var undercounts `filled_takes` against a denominator that keeps growing. The manufactured KILL again, down the SUCCESS path, and ANTI-CONSERVATIVE: it can only fire the stop EARLY, never delay it. **Defence, in code:** the leaf gains `node_store_path_check(expected_path: Path) -> Literal["MATCH", "MISMATCH", "NO_NODE", "DISCOVERY_FAILED"]` -- **FOUR states (REVISE-4, round-7)**, which (1) discovers the running `breezy-trade` process(es) by an ANCHORED argv match over `/proc/*/cmdline`. **The cmdline rule, PINNED (BLOCK-2, round-7) -- prose here is the spec the RED enforces:** read `/proc/<pid>/cmdline` as BYTES, `split(b"\0")`, **DROP the empty trailing element** the kernel's terminating NUL produces, decode each remaining element, `" ".join(...)` them, and match `re.search(r"(^|/)breezy-trade$", joined)`. The `(^|/)` alternation is what makes the anchor a whole-basename match rather than a suffix match, and the trailing `$` is what keeps the supervisor out. **Why that shape is exactly right for BOTH processes, cited:** the node's argv is the SINGLE element `str(node_bin)` (`spawn_node`, `trade_supervisor.py:506-507` -- `subprocess.Popen([str(node_bin)], ...)`), and `node_bin` is `repo_root / ".venv" / "bin" / NODE_CONSOLE_SCRIPT` (`:973`; `NODE_CONSOLE_SCRIPT = "breezy-trade"`, `:90`), so the joined string ends `/.venv/bin/breezy-trade`; the supervisor's own argv carries `SUPERVISOR_ARGV_TOKEN = "breezy-trade-supervisor-daily"` as its required first positional (`trade_supervisor_core.py:52`, enforced `trade_supervisor.py:961-968`), so its joined string ends `...-supervisor-daily` and can never match. **Same anchoring INTENT as the supervisor, cited:** `find_pid_by_argv` (`trade_supervisor.py:324-341`) is `pgrep -f` with a trailing-`$` anchor supplied by the caller, the rule documented at `:939-941` ("so `pgrep -f 'breezy-trade-supervisor-daily$'` can find exactly this process without substring-matching the node's own `breezy-trade` argv"). The leaf MIRRORS that anchoring but reads `/proc` directly rather than reusing the helper, for two stated reasons: it must handle >1 match (the helper returns only the FIRST pid, `:337-341`) and it must be testable against a fake `/proc` tree, which a `subprocess` call is not. **Never a `pgrep` on a loose pattern.** (2) reads each discovered `/proc/<pid>/environ`. **Why that read is permitted -- the rationale is the SPAWN, not the unit scope (REVISE-5, round-7):** the node is started by `spawn_node` as `subprocess.Popen([str(node_bin)], env=dict(env), cwd=..., start_new_session=True, preexec_fn=_child_preexec, close_fds=True)` (`trade_supervisor.py:489-516`, verified) -- **no `user=`/`group=` argument and no uid change anywhere in that call**, so the node runs under the SAME uid as its parent, and any process of that uid (the 14:15 scorer, the 14:30 tally) may read its `/proc/<pid>/environ`. Unit scoping (`systemctl --user`) is a consequence of the same fact, not the proof of it. It then compares that value's `POLYMARKET_US_EXEC_STATE_DB` to the scorer's resolved path. (3) returns the enum and **NEVER surfaces either value**: not in the return, not in a log line, not in an exception. **Edge states RULED, not discovered later:** a pid that vanishes between discovery and read is SKIPPED (a race, not a mismatch); an unreadable `environ` on a discovered node, or two live nodes that DISAGREE, is **MISMATCH -- fail closed**, because the check exists to PROVE the binding and "cannot prove" must be loud, never assumed MATCH. **DISCOVERY_FAILED (REVISE-4) is the fail-closed state for the SCAN itself:** `/proc` unscannable, a permission error, or any `OSError` walking it -- the check could not even enumerate candidates, so it proves nothing. **`NO_NODE` is therefore a POSITIVE result, not an error state: the scan SUCCEEDED and found zero anchored processes.** Conflating the two would let an unscannable `/proc` be read as "no node running", i.e. a silent WARN-and-continue on the exact evidence gap this check exists to close. **Scorer behaviour:** MISMATCH -> refuse `node_store_mismatch`, **non-zero, NO marker**; **DISCOVERY_FAILED -> treated EXACTLY as MISMATCH (non-zero, NO marker, REVISE-4)**, differing only in the refusal reason (`node_store_discovery_failed`) so the journal says which; NO_NODE (the scan succeeded and matched nothing) -> ONE value-free WARN line and CONTINUE, since the store may still be the right one and the positive control still applies; MATCH -> continue. **Schedule premise, stated because NO_NODE's severity depends on it (NOTE-8, round-7): at 14:15 and 14:30 UTC the node is up BY CONSTRUCTION** -- the supervisor stops the prior node at `STOP_PRIOR_UTC = 16:40` and launches at `LAUNCH_UTC = 16:50` (`trade_supervisor_core.py:31-32`, verified), so a node launched at 16:50 D-1 is still running through 14:15/14:30 D, and NO_NODE at those hours is ABNORMAL (a crash day) rather than routine. It stays a WARN, not a refusal, only because it cannot manufacture a KILL on its own. **If the supervisor schedule ever moves INSIDE 14:15-15:30 UTC, this premise dies: the pre-flight degrades to a WARN-only check on both consumers and MUST be revisited** -- a scheduled node-down window would make NO_NODE routine and the check silent exactly when it is needed. **Both consumers bound, not only the scorer:** the v1 python is BINDING and cannot gain the check, so the leaf carries a tiny `__main__` -- `"$PY" -m breezy.runtime.exec_state_db_path --check` -- and `live-tally-run.sh` runs it BEFORE the tally (change (6) below). **Its contract, frozen (REVISE-5, round-7), because a wrapper parses its output:** (i) it resolves the EXPECTED path itself, in-process, via `resolve_store_path(os.environ)` -- the same leaf function and the same env var the scorer uses, so the wrapper never passes a path in argv and the two consumers cannot be checked against different expectations; (ii) an unset or invalid value (empty, relative, or `..`-bearing) exits NON-ZERO with ONE value-free line naming the VAR `POLYMARKET_US_EXEC_STATE_DB` and which rule failed, **never the value**; (iii) on a successful check it prints **exactly ONE enum token** on stdout -- `MATCH`, `MISMATCH`, `NO_NODE` or `DISCOVERY_FAILED`, nothing else, no prefix, no trailing commentary -- and exits non-zero on MISMATCH and on DISCOVERY_FAILED, zero on MATCH and NO_NODE. **RED tests** (`tests/unit/test_exec_state_db_path.py`): a fake `/proc` tree under `tmp_path` (root injected as a keyword-only parameter defaulting to `/proc`), **one RED per enum -- all FOUR outcomes** (REVISE-4: DISCOVERY_FAILED via an unreadable `/proc` root; NO_NODE via a scannable root holding no anchored process); a vanished pid is skipped; two disagreeing nodes -> MISMATCH; **the fixture writes each `cmdline` with the TRAILING NUL exactly as the kernel does** (`b".../.venv/bin/breezy-trade\0"`), and two REDs pin the matcher: a real-shaped `.../.venv/bin/breezy-trade\0` MATCHES, and `b"breezy-trade-supervisor\0breezy-trade-supervisor-daily\0"` does NOT (BLOCK-2; the token is `SUPERVISOR_ARGV_TOKEN`, `trade_supervisor_core.py:52`); a `--check` RED for the (ii)/(iii) contract above (unset var -> non-zero, value-free, names the VAR; a successful run prints exactly one token and nothing else); and a **sentinel test** -- neither path value appears in the return, in `caplog`, or in `__main__`'s stdout for any of the four outcomes.
- **Fill-vs-ask guard (L-25).** `fill_px < entry_ask - tick` is a defect signature. `tick` is the venue's `orderPriceMinTickSize` -> `Instrument.price_increment` (`parsing.py:1274,1330`); UNVERIFIED that any artefact this offline
  reader opens carries it, so the builder declares one module constant `Decimal("0.01")` citing the evidence DIRECTORY `docs/evidence/venue/polymarket_us/`, where all 23 files naming `orderPriceMinTickSize` carry `0.01` in every
  observed market payload (the `0.005` in the API doc is an illustrative example, never an observed PM.us value). **The one-tick tolerance deliberately differs from paper replay's ZERO tolerance** (`paper_replay.py:131-143`,
  `ImpossibleFillPriceError`): replay crosses the SAME book the decision priced, where improvement is impossible, whereas a LIVE IOC can legitimately improve between decision and execution.
- **`scheduled_release_at_ns`:** derive from the registry settlement clock, not midnight UTC -- midnight UTC of `climate_day+1` precedes the venue's 08:00 America/New_York close by up to 8h (LAX/SFO).
  `registry.settlement_deadline(venue, city)` (`sites.py:416-421`) carries `settlement_time_local`/ `settlement_timezone` (:170-171) and `nws_actor._settlement_deadline_ns` (:2148-2153) already computes `climate_day+1` at that
  wall-clock in that zone; it is PRIVATE, so promote its five-line body to one shared pure helper both call, never a second copy. **NOTE:** that deadline is the VENUE's DST-following civil deadline (`sites.py:419-420`), used here as
  a PROXY for the NWS release clock the field name denotes. It is inert in v1 -- it gates only the >= +7d venue fallback, unreachable from this source (O3) -- so the proxy costs nothing today, but a real NWS release clock is
  required before that fallback is enabled.
- **Excluded-fills artefact.** `<store_dir>/excluded_fills.jsonl`, append-only, one JSON object per exclusion with keys `trial_id, station, climate_day, venue_order_id, qty, reason, filled_at_ns, scored_run_utc`; idempotent on
  `(venue_order_id, reason)`; credential-free by construction. **ONE write site**, after `score_live_trials()` returns, unioning the READER's exclusions (BLOCK-3's three join reasons) with `_admit_fill`'s (`partial_fill`/`multi_fill`/`fill_below_ask`/`fee_unverified`) -- ONE exclusion stream -- `score_live_trials()` returns ONE merged exclusion tuple and `main()` is the SOLE appender -- never two appenders, and never a refusal. Idempotence otherwise
  unchanged: the three existing guards key on `(trial_id, revision_seq/raw_sha256, score_seq)`.
- **RED tests** (new `tests/unit/test_score_live_trials_state_db_source.py`): seeded DB yields one `FilledTrial` with the latch's `entry_ask`, the record's per-contract fee and the CLI-location station; **BLOCK-3's three join events are EXCLUSIONS, and the reader returns NO refusals in any of them**: TWO
  fill records on one latch produce TWO `duplicate_fill_for_latch` lines, one per `venue_order_id`, each with its own qty/`filled_at_ns`, and NEITHER fill enters `n`; a fill with no taken latch produces ONE `no_taken_latch` line whose `trial_id`/`station`/`climate_day` are `""` while `venue_order_id`/`qty`/`filled_at_ns` are populated; an instrument mapping to >1 latch produces one `ambiguous_latch` line per fill; **each of the three exits 0** (3.0 (a)); q=0.37, `fill_below_ask` and `fee_unverified` excluded and appended; artefact idempotent across runs; two runs write one scored row; a `paper_replay/` latch key
  never matches; an open writer does not block the read; **an EMPTY-but-valid store refuses `store_positive_control_failed` (non-zero, no marker) while a store carrying ONE latch for ANOTHER manifest city PASSES the control** (BLOCK-1.2); **an unreadable/absent DB makes the scorer exit non-zero** (the wrapper, not the scorer, owns the marker).

### I3 -- scheduling and the two stop denominators (REQUIRED)
- **ONE scoring unit at 14:15 UTC**, not a step inside each wrapper: `deploy/systemd/breezy-score-live-trials.{service,timer}` + `score-live-trials-run.sh`. 14:15 is free (occupied: 09:00, 13:30, 14:30, 15:30, 22:30, 22:45, 00/06/12/18:15). **Why:** (a) single writer -- a step in both wrappers can run concurrently if 14:30 is retried while 15:30 starts, and idempotence protects content, not concurrent appends; (b) repo convention is one unit per job; (c) the excluded-fills artefact gets an owner. Give the unit an explicit `TimeoutStartSec` -- the station-day counter below calls `load_depth` per station-day and grows with the archive.
- **The path supplier (BLOCK-1.1) -- ONE line, MEASURED, absolute.** ONE line in EACH of `breezy-score-live-trials.service` (new) and `breezy-live-tally.service` (existing -- **verified: no `Environment=` today**, only the "No EnvironmentFile, deliberately: this unit holds no venue credential" comment at `:22-24`; verified only `breezy-quote-tape.service` and `breezy-quote-tape-ingest.service` carry an `Environment=` line at all today), byte-identical in both: `Environment=POLYMARKET_US_EXEC_STATE_DB=/home/jon/.local/share/breezy/state/exec_polymarket_us.sqlite` -- a non-secret path literal, NOT an `EnvironmentFile=`, NOT one of the seven enablement values.
  - **Absolute, not `%h` -- Rev 5's `%h` is DROPPED.** Repo convention for a required deploy path is an absolute literal: `breezy-quote-tape.service:63` and `breezy-quote-tape-ingest.service:30` both read `Environment=BREEZY_POLYMARKET_US_QUOTE_TAPE_CATALOG=/home/jon/...`, byte-identical to each other for exactly this reason. And **every Breezy unit is a `systemctl --user` unit** (`breezy-live-tally.service:40` "`systemctl --user enable breezy-live-tally.timer` is the only thing to enable"; `deploy/systemd/README.md:38-45` symlinks the units into `~/.config/systemd/user/` and runs `systemctl --user daemon-reload`; verified NO unit under `deploy/systemd/` references `/etc/systemd/system`), so the unit's `%h` and the operator's `$HOME` are the SAME home -- `%h` would expand to this exact string with one more indirection and one more thing to reason about.
  - **Provenance of the literal: MEASURED, never guessed.** Coordinator, 2026-09-05 ~02:30 UTC: the RUNNING node's (`breezy-trade`, the supervisor-adopted process) `POLYMARKET_US_EXEC_STATE_DB` was COMPARED to this candidate string **without printing either value** -- byte-identical, with no `~`, no `$HOME` and no `%h` in it; that file exists and is the store holding the 4 measured rows (3 `current_rung_hold/trial/*` latches + `durability:probe`, **0 fills**). Measurement is mandatory rather than fastidious: `factories.py:183-187` documents the var as "REQUIRED. Absolute path ... no default, and never guessed, because a wrong path silently starts a fresh, empty store" -- and a fresh empty store is precisely BLOCK-1.2's manufactured KILL. **Build-completion criterion, restated: no `EnvironmentFile=`, no venue credential, no enablement value** (a non-secret `Environment=` path line IS allowed); outputs still carry only trial_id/station/climate_day/venue_order_id/qty/reason. **Deviation stated, not hidden:** the supervisor's "no value is ever written to argv, a file, or a log record" posture (`trade_supervisor.py:9-15`, resolver :923-933) governs the seven ENABLEMENT values; a state-DB path is not one of them, is already a unit-file literal, and reaches `live_family_tally.py`'s argv as `--fill-source`.
- **One supplier, TESTED (BLOCK-1.3).** Two `Environment=` lines plus this plan is three definitions of one string; a test, not prose, collapses them. The new deploy test module asserts the `Environment=POLYMARKET_US_EXEC_STATE_DB=` line in `breezy-score-live-trials.service` and in `breezy-live-tally.service` are **byte-identical to each other AND equal to the pinned literal above**, so drift fails RED rather than silently splitting the two consumers onto different stores. **A shared `.d/` drop-in was considered and NOT chosen:** it introduces a THIRD file plus a symlink step in `README.md:38-45` to remove a duplication the equality test already makes undriftable -- more deploy surface for less. **Runbook (procedure, not code): a VALUE-FREE pre-deploy re-check** -- read the running node's value from `/proc/<node pid>/environ`, compare it to the unit literal, and print **only `MATCH` or `MISMATCH`**, never the value; a MISMATCH blocks the deploy. Same posture as I1b's read-only fill-count re-check. **The FOURTH definition -- the NODE's own launch environment -- is bound by the REVISE-1 pre-flight at EVERY RUN, not only at deploy.** The two unit literals and this plan are collapsed by the equality test above, but the RUNNING node's value is a fact no unit-file test can see; `node_store_path_check` is what binds it, value-free, on every scorer run and (via `-m ... --check`) every v1 tally run. The runbook step above stays as the DEPLOY-time cross-check -- it catches a wrong literal BEFORE the first run -- and the pre-flight is the per-run one; neither replaces the other.
- **Ordering: a dated success marker, not `After=`; ONE writer (BLOCK-2).** `After=` does not order units started by SEPARATE timers, so the Rev 1 proposal was inert. **`score-live-trials-run.sh` -- never the scorer python -- writes `score_live_trials_ok_$STAMP`** under the shared `BREEZY_LIVE_TALLY_OUTPUT_DIR`, and only after EVERY city's invocation exited 0; a non-zero exit anywhere leaves no marker and fails the wrapper. BOTH tally wrappers assert today's marker and exit non-zero with a value-free log line otherwise. `OnSuccess=` rejected: it makes the scorer START the tallies, replacing two independently retryable timers with a chain whose cadence no longer reads off the timer files. **The wrapper loops the manifest's stations** (3.0 (e)) and hands that SAME `"$FAMILY_MANIFEST"` to EVERY scorer invocation. **Two wrappers, ONE tested literal (BLOCK-1, round-7):** the counter is invoked by `live-tally-run.sh`, not by this one, so the identical assignment `FAMILY_MANIFEST="$REPO/deploy/families/pm_us_crh_v2.json"` appears in BOTH wrappers -- repo-relative from the `REPO=/home/jon/breezy` each already derives (`live-tally-run.sh:10`, verified) -- and a RED asserts the two assignments are BYTE-IDENTICAL, the same shape BLOCK-1.3 applies to the `Environment=` line. Prose cannot hold two files together; a test can -- `score_live_trials` takes one `--city` and opens one catalog (:419-420); other cities' fills would emit spurious `instrument_unavailable`. I2's city filter is the other half.
- **O1 inside I3 -- both stop flags, ONE window (A3).** `live-tally-run.sh` currently passes neither (:23-26). The literal invocation it must make -- **all three flags VERIFIED pre-existing** on `live_family_tally.py:454-471`, so this is a wrapper change only:
  ```
  live_family_tally.py "$STORE_DIR" --output "$OUT/live_family_tally_$STAMP.md" --as-of "$STAMP" \
      --fill-source "$STATE_DB" --fill-since-climate-day "$D0" --covered-listed-station-days "$COUNT"
  ```
  Without them `filled_takes=None` or a `None` denominator leaves the stop unevaluated (`structural_dead_stop.py:95-116`). `$STATE_DB` is BLOCK-1's env var; `$D0` and `$COUNT` come from ONE file, below.
  - **d0 plumbing: `structural_dead_stop.py` gains `--family-manifest <path>` and `--output <path>` -- NOT `--fetch-start`.** It loads the manifest with the shipped strict loader `load_family_manifest` (`persistence/family_manifest.py:115`, **no `allow_draft`**), uses `fetch_start = manifest.d0_climate_day` and `cities = manifest.stations` (:107,110), and echoes `fetch_start` + `manifest_sha256` (:112) into the `--output` JSON. Passing the manifest rather than the date makes the census (3.0 (e)) and the window ONE fact from ONE registered file. **Verified:** `_parse_args` (:198-206) takes `--catalog-root` only and `main()` (:209-215) forwards ONLY `catalog_root` to `count_covered_listed_station_days_from_catalog` (:165-171, whose `cities`/`fetch_start` default to `DENSE_STATIONS`/`ASOS_FETCH_START`), so `main()` MUST forward both or the flags are INERT and the wrapper silently keeps the pre-live ARCHIVE window -- `ASOS_FETCH_START` = 2026-08-30 (`ma_prelock_winner_ask_study.py:184`) returns >= the floor 15 (`structural_dead_stop.py:73`) against ZERO live fills and fires `structural_dead` on the first evaluation: **a manufactured KILL.** The defaults are untouched, so the M_A study path still runs on `(ASOS_FETCH_START, DENSE_STATIONS)`; adding flags to an analysis script is not a v1-module change.
  - **the ONE source of that date: the REGISTERED `deploy/families/pm_us_crh_v2.json` (`:5`, `d0_climate_day = "2026-09-05"`, `status: "REGISTERED"`). NO `pm_us_crh_v1.json` is authored** -- three verified reasons: (i) the loader REQUIRES `boundary_artefact_path` + a 64-hex non-placeholder `boundary_inputs_sha256` (:53-64,163-172), which a Wilson rule has none of -- any value would be FABRICATED provenance; (ii) any `deploy/families/*.json` whose `family_id` equals its stem is a VALID argument to the v2 tally wrapper (`family-tally-v2-run.sh:42-51,58-64`), silently offering a v1 id to a v2 boundary solver; (iii) it would be byte-identical to v2 in `(trial_id_prefix, d0_climate_day, stations)` -- the pair `family_barrier.py:15-22` says the discriminant CANNOT separate. **Semantics, so no reader mistakes a v2 fact for a v1 one:** v2 §8:175 registers v2 as a NEW family (`climate_day >= d0_utc_day`, pre-D0 fills v1-only, :196-197); v1's earliest D0 is the SAME date (PREREG v1 §6:130) and ZERO fills exist, so the row sets COINCIDE -- they are not one family -- and this is the only REGISTERED machine-readable pin. The counter refuses (non-zero) if the manifest is absent, unreadable or not `REGISTERED`, never falling back to `ASOS_FETCH_START`. **Drift guard:** the wrapper carries `V1_D0_LITERAL="2026-09-05"  # PREREG v1 §6:130` and refuses (non-zero, one value-free line) if the echoed `fetch_start` differs -- v1's own D0 is prose, so a later v2 amendment must not silently re-window the v1 stop.
  - **the wrapper reads ONE file for BOTH numbers (R9).** `--output` writes 3.0 (i)'s six keys under the shared output dir; the wrapper `sed`s `count` AND `fetch_start` out of it (never the human print at :214, never a stdout scrape, never `$PY -c`) and passes that same `fetch_start` string to `--fill-since-climate-day`. **The scorer is NOT given a date (REVISE-6):** it reads `d0_climate_day` from the manifest itself, and the RED `fetch_start == manifest.d0_climate_day` is what makes the v1 stop's window and the scorer's window provably ONE date without a second flag to keep in step. A retry re-reads the file instead of re-walking the catalog.
  - **`fetch_end` and day one (R3).** The upper bound stays `ASOS_FETCH_END = default_asos_fetch_end()` = `date.today()`, frozen at IMPORT time (`ma_prelock_winner_ask_study.py:168-185`); the numerator has no upper bound, so a fill on a day not yet counted only DELAYS a KILL -- conservative. **Inverted day-one range (`fetch_start > fetch_end`): verified from source** -- `discover_station_days` (`ma_prelock_winner_ask_study.py:547-560`) iterates `while day <= fetch_end`, so an inverted range never enters the loop and returns `()` -> count 0, no exception.
  - **Absent depth root.** `discover_station_days` calls `depth_root.iterdir()` FIRST (:551), so a missing `<catalog_root>/data/order_book_depths` (`structural_dead_stop.py:179`) raises. The counter CATCHES that ONE case and writes `{"count": 0, "depth_root_present": false, ...}`, exit 0; the wrapper passes `--covered-listed-station-days 0` and emits one value-free WARN line. Conservative and self-explaining: 0 is below the floor 15 so the stop cannot fire, and the JSON says why. **Alternative rejected:** failing the whole tally would take the only daily v1 statistic offline for a quote-tape outage that cannot manufacture a KILL. The counter's own caveat (:136-151) carries over -- an outage day is indistinguishable from a never-listed day, under-counting and DELAYING a KILL, which is only true once the window itself is right.
- **The v1 wrapper's change list** (all wrapper-level; `live_family_tally.py` SOURCE untouched): (1) `PY="${BREEZY_LIVE_TALLY_PYTHON:-$REPO/.venv/bin/python}"` replacing the hard-coded `PY="$REPO/.venv/bin/python"` (`live-tally-run.sh:11`), mirroring `BREEZY_FAMILY_TALLY_V2_PYTHON` (`family-tally-v2-run.sh:16`) so RED (i) can assert byte-identity against a shell STUB; (2) `STATE_DB="${POLYMARKET_US_EXEC_STATE_DB:?}"`; (3) `FAMILY_MANIFEST="$REPO/deploy/families/pm_us_crh_v2.json"` (byte-identical to `score-live-trials-run.sh`'s, BLOCK-1) + the counter invocation with `--family-manifest "$FAMILY_MANIFEST"` + the one-file read + the drift guard; (4) the marker assertion; (5) the three flags on the fenced invocation; (6) **`"$PY" -m breezy.runtime.exec_state_db_path --check` BEFORE the tally invocation (REVISE-1)**, exiting non-zero with one value-free line on MISMATCH **and on DISCOVERY_FAILED** (REVISE-4), continuing on MATCH and on NO_NODE; the wrapper reads the ONE enum token that command prints and never a path (REVISE-5).
- **The v2 wrapper's change list** (`family-tally-v2-run.sh`, 84 lines today; `family_tally_v2.py` SOURCE untouched): exactly ONE change -- (1) assert today's `score_live_trials_ok_$STAMP` under `$OUT` before invoking the CLI, exiting non-zero with one value-free line when it is absent. Both names already exist (`family-tally-v2-run.sh:70` `STAMP`, `:24` `OUT`), so this is an insertion, not a restructure. **It needs NO `POLYMARKET_US_EXEC_STATE_DB` line and NO `--fill-source`:** the v2 tally reads the SCORED store (`:23` `STORE_DIR`), never the exec DB, so `breezy-pm-crh-v2-tally.service` gains no `Environment=` line (**verified: it carries none today**) and BLOCK-1.3's byte-identity test covers exactly two units, not three. **Its test:** `tests/unit/test_family_tally_v2_deploy.py` (174 lines) gains the marker RED -- absent marker => non-zero and the stub is NEVER invoked; present marker => the stub runs -- reusing that module's shipped `BREEZY_FAMILY_TALLY_V2_PYTHON` stub idiom (`:40-47,95-99,127-133`).
- **Tests.** Extend `tests/unit/test_deploy_timer_hours.py` (14:15 owned by one timer); new wrapper test shaped like `test_family_tally_v2_deploy.py` (invalid/missing city fails loudly; failing python exits 1; artefacts under the shared output dir; missing marker => non-zero from BOTH tally wrappers; **the marker appears only after the LAST city, and a mid-loop non-zero leaves none**). New `tests/unit/test_structural_dead_stop_cli.py` for the CLI -- **correction to the round-4 premise: `tests/unit/test_structural_dead_stop.py` DOES exist (172 lines) but covers only the PURE functions** (`structural_dead`, `covered_listed_station_days`; module loaded via `importlib.util` as `sds_mod`; no `main`/`_parse_args`/argv test), so the CLI is uncovered today and the new module reuses that same loader idiom. **RED tests:** (i) **`fetch_start` in the counter's `--output` JSON equals `load_family_manifest(pm_us_crh_v2.json).d0_climate_day` for the SAME manifest** (REVISE-6), and the date the wrapper passes to `--fill-since-climate-day` is byte-identical to that `fetch_start` -- the scorer takes NO date flag, so there is no third string to keep in step; (ii) an absent/DRAFT/unreadable manifest exits non-zero; (iii) `main()` FORWARDS the manifest's date AND stations to the counter (a silent no-op fails); (iv) an inverted range counts 0 and does not raise; (v) census identity `set(DENSE_STATIONS) == set(load_family_manifest(pm_us_crh_v2.json).stations)` (R2); (vi) an absent depth root -> `count=0`, `depth_root_present=false`, exit 0; (vii) the drift guard fires when the manifest's d0 differs from `V1_D0_LITERAL`; **(viii) the two units' `Environment=POLYMARKET_US_EXEC_STATE_DB=` lines are byte-identical to each other and equal to the pinned absolute literal (BLOCK-1.3), and `breezy-pm-crh-v2-tally.service` carries no `Environment=` line at all; (ix) the scorer's exit-code contract at the wrapper boundary -- a stub exiting 0 having scored nothing (zero fills, or every row refused `malformed_input`) still yields the marker, a stub exiting non-zero yields none** (3.0 (a), BLOCK-b); **(x) the v1 wrapper aborts non-zero and NEVER invokes the tally when the `--check` stub reports MISMATCH or DISCOVERY_FAILED, and DOES invoke it on MATCH and on NO_NODE** (REVISE-1/REVISE-4, stubbed through the same `BREEZY_LIVE_TALLY_PYTHON` seam as (i)); **(xi) the `FAMILY_MANIFEST=` assignment line in `score-live-trials-run.sh` and in `live-tally-run.sh` is byte-identical between the two files** (BLOCK-1, round-7 -- the counter lives in the v1 wrapper, the scorer loop in the new one, so the literal exists twice and ONE test collapses it), **and within `score-live-trials-run.sh` that same `"$FAMILY_MANIFEST"` reaches every scorer invocation** (BLOCK-a). Verified: NO test module references `live-tally-run.sh` today, so the v1 wrapper's assertions need a new module.

### I3c -- v2 coverage table reads the excluded-fills artefact (REQUIRED, small)
The v2 registration ack commits to a "coverage table" (`docs/evidence/grok_v2_registration_ack_2026-09-05.md:18`, C3) with `dn = dk = dI = dS = 0`. **Verified: `family_tally_v2.py` contains no
coverage-table implementation today**, so this consumer is new work, not a wiring change. It reads `<store>/excluded_fills.jsonl` and renders counts by reason/station/climate_day, changing no statistic and no v2
boundary; `live_family_tally.py` untouched (BINDING). **Count rule (strategy-lead Q2: "append-only jsonl is evidence, not the count"):** ONE entry per `venue_order_id`, the latest line by `scored_run_utc` wins (a plain string
compare -- 3.0 (g)), and an exclusion whose `trial_id` has a scored row is DROPPED from the count -- "has a scored row (any `score_seq`)" being asked of the SHIPPED reader `read_scored_trials(derived_dir)`
(`persistence/scored_trial_store.py:104`, 3.0 (h)), never a second parquet reader -- so a later attested trial leaves the table automatically and nothing is edited. **Test modules:** `tests/unit/test_family_tally_v2.py` for the
counting rule, plus `tests/unit/test_family_tally_v2_truncation.py` if the table reaches the rendered report (**verified it exercises `render_markdown_v2` at :170,185**; the renderer is `family_tally_v2.py:589`). RED tests both
sides: the artefact's shape; the table from a seeded artefact; two lines for one `venue_order_id` count once (latest reason); an exclusion whose `trial_id` is scored is not counted. **The table counts BLOCK-3's reasons too** -- `duplicate_fill_for_latch`, `no_taken_latch` (whose `trial_id`/`station`/`climate_day` are `""`, so it groups under a blank station rather than being dropped) and `ambiguous_latch` are ordinary members of the widened `FillExclusionReason` set and need no separate rendering path.

### I5 -- end-to-end contract test (REQUIRED; was O6)
One test under `tests/contract/test_live_fill_scoring_chain_contract.py` driving the REAL submit chain with a schema-shaped 200 create-order fixture through `record_fill` -> state DB ->
`read_filled_trials_state_db` -> `score_trial` -> a scored row and a tally denominator; otherwise each increment is green in isolation and the chain is unproven. **X1 widening, old -> new** (exact set at
`tests/unit/test_execution_egress_firewall_guard.py:2673-2689`, verified -- note the module lives under `tests/unit/`, not `tests/contract/`): the set gains `tests/contract/test_live_fill_scoring_chain_contract.py` AND
`tests/unit/test_score_live_trials_state_db_source.py`, and BOTH are UNCONDITIONAL because this plan MANDATES that each imports `DurableFillRecord` from `exec.client` directly to seed the DB -- seeding from a hand-written JSON
blob would test a second, drifting copy of the encoding. **Correction to the round-4 rationale:** the trigger is NOT transitive reachability -- `_imports_exec_package` / `exec_importing_test_modules` (:1152-1169, :2605-2611) parse
each TEST module's OWN import statements, so the set is computed from that mandated import line and a module that merely drives the scorer would not qualify (adding it would FAIL the equality, not widen it). The comparison stays
`==`; widened, never relaxed (L-12).

### I4 -- real `generate_fill_reports` -- DEFERRED, not required for the goal
The synchronous IOC path (`submit_chain.py:478-493`) means every fill Breezy can have is in hand at `_submit_order`, and I1b captures it; the POSITION is already reported by the real
`generate_position_status_reports` (:1015-1055), so deferring costs no exposure visibility. Native reconciliation adds value only for a latch-armed-but-unretired order (the AMBIGUOUS walk), where
by-id `generate_order_status_report` (:938-985) is already the mechanism. Deferred with the exact-set B6/B9 analysis it needs.

## 4. Omission hunt -- what is missing and would block the goal

- **O1 [blocks the v1 stop]. `live-tally-run.sh` passes NEITHER stop flag** (:23-26). Fixed in I3, together with the window defect (A3) that would otherwise turn the fix into a false KILL.
- **O2. Nothing schedules `score_live_trials.py`** -- both tallies read a store no automated job writes; without I3, I1a+I1b+I2 are inert.
- **O3. The venue last-fair-price fallback is unreachable from this source.** The reader always yields `venue_settlement_tmax_f=None`, so `_resolve_settlement_basis` (`trial_scorer.py:211-242`) can never take that branch. Acceptable
  and honest (a permanently-refused trial is visible), but stated, not discovered later.
- **O4. No fee on `DurableFillRecord`.** Without I1a/I1b the scorer must re-derive the fee from the schedule -- a computed number wearing the grammar of a venue report (A13).
- **O5. Corrections accessor: a documented v1 property, not a defect.** `read_climate_day_including_corrections` warns "never call this from a settlement, reconciliation or retry path" (`catalog.py:633-635`) -- i.e. never replicate
  the venue's payout. PREREG v1 §4 (frozen, `docs/specs/PREREG_v1_current_rung_hold_2026-09-04.md:76-93`) deliberately measures NWS truth: FINAL print only, superseded refused, "a re-scored trial counts once, at max `score_seq`", so
  the driver's existing call (`score_live_trials.py:472-474`) IS the v1 rule. **Consequence:** on a corrected station-day the scored P&L may differ from the venue payout -- a blocker-register NOTE for operator reporting, no ruling
  and no code change.
- **O6 -- CLOSED, promoted to increment I5.**

## 5. Risks and operator-visible behaviour

- **[HIGH] Silent exclusion.** `main()` prints the excluded-fills table (`score_live_trials.py:554-560`) but `live-tally-run.sh` sends stdout to `/dev/null` (:26). I2's artefact plus I3c's table are the fix -- a partial fill absent
  from both the scored store and the log is an unrecorded real trade.
- **[HIGH] Fill without evidence.** If `record_fill` raises (unreadable index -> `PolymarketUSError`, `client.py:1331-1336`) the order has already filled at the venue. I1b's control flow handles it: refuse, degrade, intent stays
  OPEN, still publish BOTH events.
- **[HIGH] Manufactured KILL.** Passing `--covered-listed-station-days` computed over the default archive window would fire the v1 structural-dead stop on day one with zero live fills. Fixed in I3 (A3), pinned by a window-equality
  RED test.
- **[HIGH] Wrong-but-openable state DB (BLOCK-1.1/1.2).** A path that opens cleanly but is the WRONG file yields 0 fills, exit 0, a marker, and `filled_takes=0` into the v1 stop against a >= 15 denominator -- the manufactured KILL again, but arriving down the SUCCESS path where no error surfaces. Three defences, in order of the failure they intercept: (1) the MEASURED absolute unit literal (I3 BLOCK-1.1) -- **coordinator, 2026-09-05 ~02:30 UTC, compared the RUNNING `breezy-trade` node's `POLYMARKET_US_EXEC_STATE_DB` to the candidate string without printing either: byte-identical, file present, and it is the store holding the 4 measured rows (3 latches + probe, 0 fills)**; (2) the byte-identity test across the two units (BLOCK-1.3), so the two consumers can never split; (3) the store positive control (I2 BLOCK-1.2), which REFUSES a store holding no family latch for any manifest station at all. **(4) the REVISE-1 node-env pre-flight.** Residual stated honestly: defence (3) does NOT survive a stale COPY of the store, nor an operator re-pathing the RUNNING node -- the old file keeps its latches, so it PASSES the positive control, exits 0, writes the marker, and feeds the v1 wrapper an undercounted `filled_takes` against a denominator that keeps growing: the manufactured KILL down the SUCCESS path, and ANTI-CONSERVATIVE (it can only fire the stop EARLY, never delay it). (4) compares the RUNNING node's env value to the scorer's resolved path and returns MATCH/MISMATCH/NO_NODE/**DISCOVERY_FAILED** (REVISE-4) **without surfacing either value**; MISMATCH and DISCOVERY_FAILED refuse (non-zero, no marker) -- an unscannable `/proc` proves nothing and must not read as "no node running" -- while NO_NODE (scan succeeded, nothing anchored) warns once and continues. (3) is a store-CONTENT fact and (4) a LIVE-PROCESS fact -- neither subsumes the other, and both are code, not runbook steps.
- **[NOTE] ONE rule for path logging (NOTE-3).** The scorer logs the RESOLVED fill-source path exactly once per invocation -- a repo-pinned, non-secret filesystem literal (see the deviation NOTE below), so a wrong-store incident is diagnosable from the journal -- while the REVISE-1 `/proc` comparison prints ONLY the enum `MATCH`/`MISMATCH`/`NO_NODE`/`DISCOVERY_FAILED` -- exactly one token, REVISE-5 -- and never either value: the resolved path is this repo's OWN pinned literal, whereas the node's `environ` is another process's memory, which Breezy reads to COMPARE and never to REPORT. Never repeated per record, never written into an artefact; the deploy-time `/proc/<pid>/environ` MATCH/MISMATCH runbook step (I3) is unchanged.
- **[MED] New reachable state: record exists + intent OPEN.** A crash between the fill write and `_retire` leaves durable evidence and an armed intent. `probe_open_intent` (`trade_supervisor.py:297`) surfaces it pre-launch and the
  pinned `clear_submit_intent_cli` is the single clearing caller, so the cost is bounded at ONE trading day -- no double-submit, because a latched OPEN intent denies. The fill probe stays the constant-`False` stub
  (`client.py:695-698`): it is keyed by intent fingerprint, and `DurableFillRecord` carries no fingerprint, so making the probe real needs a NEW fingerprint index -- legitimately I4.
- **[MED] Census asymmetry, numerator vs denominator (R2).** The denominator counts only `DENSE_STATIONS` (`structural_dead_stop.py:168`; = registry cities minus contaminated ones, `cli_basis_setup_win_rate_study.py:126-128`), while
  the numerator `count_filled_takes` (`fill_time_count.py:101-158`) has NO station parameter and counts every taken trial under the prefix. **Direction: a fill outside the census can only DELAY a KILL, never manufacture one** -- it
  adds to the numerator and never to the denominator. No v1-adjacent code change; the I3 census-identity RED test pins the two sets equal. **Verified unreachable today:** the live composition builds one strategy per
  `SUPPORTED_STATIONS = ("LAX","MDW","MIA","SFO")` (`strategy/current_rung_hold/config.py:74-76`; `composition.py:220-229`, `stations=(station,)`) -- NYC/KNYC is excluded by construction, so the live node subscribes no station
  outside the census (the v2 ack records the same census, C2).
- **[NOTE] Different bounds, different sources (R3, N3).** The denominator's upper bound is `date.today()` at import (`ma_prelock_winner_ask_study.py:168-185`) while the numerator is unbounded above -- conservative in the same
  direction. The two counts also read DIFFERENT stores: the denominator the quote-tape Parquet catalog, the numerator the exec-state SQLite, so a recorder-outage day carrying a real fill is numerator-only -- again a delay, never a
  manufactured KILL.
- **[NOTE] Schedule premise behind NO_NODE (NOTE-8).** The pre-flight's leniency towards NO_NODE rests on a schedule fact, so the fact is stated rather than assumed: the supervisor stops the prior node at `STOP_PRIOR_UTC = 16:40` UTC and launches at `LAUNCH_UTC = 16:50` UTC (`trade_supervisor_core.py:31-32`, verified), so **at 14:15 and 14:30 UTC the node is up BY CONSTRUCTION** and NO_NODE at those hours is ABNORMAL -- a crash day -- not routine. **If that schedule ever moves inside the 14:15-15:30 UTC band, the pre-flight degrades to a WARN-only check on both consumers and this ruling MUST be revisited:** a scheduled node-down window would make NO_NODE routine and silence the check exactly when the store binding is least certain.
- **[MED] Concurrent reader at 14:15.** The scorer reads the live node's state DB; read-only URI + no flock is the shipped idiom, REUSED from `fill_time_count._open_readonly`. Safe by WAL + `synchronous=FULL`
  (`runtime/sqlite_store.py:123-124`) and the latch has no prune path (`trial_day_latch.py:197-239`); the "open writer does not block the read" test stays.
- **[NOTE] `fill_px` is not an independent check.** `cumulative_cost/cumulative_qty` reconstructs the venue's own `avgPx` BY CONSTRUCTION (I1a sources cost as `avgPx*cumQuantity`). It is a faithful re-expression, never a second
  opinion on the venue's price.
- **[NOTE] Event loop.** `_store_set` is a synchronous WAL commit already performed twice in this coroutine (`_latch.arm` :1556, `_retire` :1594). I1b adds a third; no executor -- offloading would break the very ordering this
  increment exists to guarantee.
- **[NOTE] The one deliberate posture deviation (BLOCK-1).** The exec state-DB PATH now appears as the MEASURED absolute `Environment=` literal `/home/jon/.local/share/breezy/state/exec_polymarket_us.sqlite` in two unit files and, from the v1 wrapper, in `live_family_tally.py`'s argv as `--fill-source`. The
  "never in argv, a file, or a log record" rule (`trade_supervisor.py:9-15`) governs the SEVEN operator-reserved ENABLEMENT values; a non-secret path is not one of them and was already a unit-file literal. No `EnvironmentFile=`,
  no venue credential and no enablement value is added anywhere.
- **Unchanged invariants (honoured by this Rev 8):** `allow_short=False`; no new `ScoredTrial` column; `trial_scorer.score_trial`, `live_family_tally.py` SOURCE and the 17-column store untouched (PREREG v1 BINDING) -- only the v1
  WRAPPER `live-tally-run.sh` gains flags; no safety/settlement/contract test weakened or deleted, exact-set barriers widened old -> new only (L-12); no operator-reserved control (budget, per-position cap, enablement) set or read;
  NO-SEND execution-egress firewall untouched; no secret, no venue credential and none of the seven enablement values in any file this plan creates -- the one new literal is a measured, non-secret filesystem path; no fabricated
  provenance (every measurement retained from Rev 6/7 names its date, method and what was NOT printed, and **Rev 8 adds no new measurement** -- every citation it adds is a source-line read of the working tree); I1a/I1b/I5 and the BLOCK-2 marker wording are untouched by this revision, and **I3c gains exactly ONE sentence** (the coverage table counts BLOCK-3's three new reasons) and is otherwise untouched. No new `ScoredTrial` column: BLOCK-3's two new fields land on the DRIVER-LOCAL `FillExclusion`, never on `ScoredTrial`, `FilledTrial` or the 17-column store.

## 6. Rev 8 -- round-7 peer-review dispositions

| # | Round-7 finding | Disposition | Where |
|---|---|---|---|
| BLOCK-1 | The manifest path was described as "ONE path, three consumers", but the counter is invoked by `live-tally-run.sh` and the scorer by `score-live-trials-run.sh` -- the literal exists in TWO files with only prose holding them together | APPLIED as **one manifest literal, two wrappers, one test**. Both wrappers carry the byte-identical `FAMILY_MANIFEST="$REPO/deploy/families/pm_us_crh_v2.json"`, repo-relative from the `REPO=` each already hard-codes (`live-tally-run.sh:10`, `family-tally-v2-run.sh:15`) (**verified: `live-tally-run.sh:10`; sibling idiom `family-tally-v2-run.sh:15`**), and a RED in the new deploy test module asserts the two assignments are BYTE-IDENTICAL -- the BLOCK-1.3 shape, applied to the manifest instead of the `Environment=` line. Within `score-live-trials-run.sh` that one variable still reaches every scorer invocation (BLOCK-a intact) | 3.0 (e); I3 ordering; I3 v1 change list (3); I3 RED (xi) |
| BLOCK-2 | "Anchored argv match" was an intention, not a rule a builder could implement identically twice | APPLIED as a **pinned cmdline rule**: read `/proc/<pid>/cmdline` BYTES, `split(b"\0")`, DROP the empty trailing element, decode, join with `" "`, match `re.search(r"(^|/)breezy-trade$", joined)`; the fake-`/proc` fixture MUST write the trailing NUL exactly as the kernel does. Cited as correct for both processes: node argv is the SINGLE element `str(node_bin)` (`spawn_node`, `trade_supervisor.py:506-507`) with `node_bin = repo_root/".venv"/"bin"/NODE_CONSOLE_SCRIPT` (`:973`, `:90` = `"breezy-trade"`); the supervisor carries `SUPERVISOR_ARGV_TOKEN = "breezy-trade-supervisor-daily"` (`trade_supervisor_core.py:52`, enforced `:961-968`). REDs: real-shaped `.../.venv/bin/breezy-trade\0` MATCHES; `breezy-trade-supervisor\0breezy-trade-supervisor-daily\0` does NOT. **The Rev 5 marker wording is untouched** -- this BLOCK-2 is the round-7 item, not the closed round-4 one | I2 REVISE-1 bullet; I2 REVISE-1 REDs |
| BLOCK-3 | Fill-level integrity events were routed to `ScoreRefusal`, which **cannot** populate the 8-key artefact: verified it carries only `trial_id, reason, detail` (`trial_scorer.py:141-147`) and is v1-BINDING in the AST-pure `settlement` package | APPLIED -- those events become **EXCLUSIONS, and the reader emits NO `ScoreRefusal` at all**. (i) >1 fill on ONE latch -> `duplicate_fill_for_latch` for EACH fill, one line per `venue_order_id` with its own qty/`filled_at_ns`, **both out of `n`**; (ii) a fill with no taken latch -> `no_taken_latch` with `trial_id`/`station`/`climate_day` = `""` (**empty strings allowed for that reason ONLY**; the ids that identify the fill are always populated); (iii) instrument -> >1 latch -> `ambiguous_latch` per fill. Signature becomes `-> tuple[tuple[FilledTrial, ...], tuple[FillExclusion, ...], Mapping[str, tuple[bool, str]]]`. Closed set widened old -> new: `{partial_fill, multi_fill}` -> `+ {fill_below_ask, fee_unverified, duplicate_fill_for_latch, no_taken_latch, ambiguous_latch}`. The driver-local `FillExclusion` gains `venue_order_id`/`filled_at_ns` (defaulted, so the three shipped construction sites stay green -- verified complete: `_admit_fill:238-252`, `test_score_live_trials.py:466,475,489`); **no v1 struct, no `ScoredTrial` column**. **Exit-code contract unchanged: exclusions are exit 0** | 3.0 (c); I2 join bullet; I2 exclusion layering; I2 R6 disclosure; I2 artefact write site; I2 REDs; I3c (one sentence) |
| REVISE-4 | A three-state enum conflated "could not scan `/proc`" with "no node running", so an unscannable `/proc` would WARN-and-continue | APPLIED -- fourth state **`DISCOVERY_FAILED`** (`/proc` unscannable, permission error, any `OSError` walking it), **treated EXACTLY as MISMATCH** (non-zero, NO marker), differing only in the refusal reason so the journal says which. **`NO_NODE` now means the scan SUCCEEDED and found zero anchored processes** -- a positive result, not an error. One RED per enum, all four | I2 REVISE-1 bullet + REDs; I3 v1 change list (6); I3 RED (x); 3.0 (a) non-zero list; section 5 (4) + NOTE-3 |
| REVISE-5 | The `--check` entrypoint had no contract (a wrapper parses its output), and same-uid readability of `/proc/<pid>/environ` was justified by unit scope rather than by the spawn | APPLIED. Contract frozen: resolves the expected path itself via `resolve_store_path(os.environ)` (never a path in argv, so both consumers are checked against ONE expectation); unset/invalid (empty, relative, `..`) -> non-zero with ONE value-free line naming the VAR and the failed rule; **prints exactly ONE enum token** on stdout, non-zero on MISMATCH and DISCOVERY_FAILED. Rationale corrected to the SPAWN: `spawn_node` runs `Popen([str(node_bin)], env=dict(env), start_new_session=True, preexec_fn=_child_preexec, close_fds=True)` (`trade_supervisor.py:489-516`, verified) with **no uid change**, so same-uid readability follows from the launch itself; unit scope is a consequence, not the proof | I2 REVISE-1 bullet; I3 v1 change list (6); section 5 NOTE-3 |
| REVISE-6 | d0 reached the scorer as a CLI string scraped from the counter's JSON -- a second copy of a fact the manifest already holds | APPLIED -- **`--since-climate-day` is DROPPED from the scorer CLI**; the scorer loads the manifest strictly and uses `manifest.d0_climate_day` (`family_manifest.py:107`; `pm_us_crh_v2.json:5` = `"2026-09-05"`, verified) as `since_climate_day`. `--family-manifest` is REQUIRED on BOTH sources (fixtures point at a test manifest). The counter still echoes `fetch_start`, with a RED pinning `fetch_start == manifest.d0_climate_day` for the same manifest -- so the v1 wrapper's `--fill-since-climate-day`, still read from that JSON, is the SAME value by test rather than by convention, and there is no third string to keep in step | 3.0 (a); I2 signature; I3 one-file read; I3 RED (i) |
| REVISE-7 | The clear-intent CLI was said to need "no new branch" AND to keep its message "unchanged" -- contradictory once the resolver RAISES | APPLIED, contradiction WITHDRAWN. Verified: `clear_submit_intent_cli.py:116-120` has NO validation today and refuses via `if not raw_state_db:` -> value-free print + `EXIT_REFUSED`. Adopting the raising resolver adds exactly ONE `try/except ExecStateDbNotConfiguredError` mapping to the SAME `EXIT_REFUSED` and the same value-free one-line print SHAPE, whose text now names the VAR and which rule failed (the fixed "is unset" wording would be false for a relative path). RED asserts exit code + value-free, NOT "message unchanged", plus a RED that the measured absolute live path `/home/jon/.local/share/breezy/state/exec_polymarket_us.sqlite` (no `~`, no `$HOME`, no `..`) is ACCEPTED -- the new validation provably cannot lock the operator out of the running store | I2 REVISE-2 bullet |
| NOTE-8 | NO_NODE's WARN-and-continue leniency rested on an unstated schedule premise | APPLIED as an explicit premise + a revisit trigger: the node is up at 14:15/14:30 UTC BY CONSTRUCTION (`STOP_PRIOR_UTC = 16:40`, `LAUNCH_UTC = 16:50`, `trade_supervisor_core.py:31-32`, verified), so NO_NODE at those hours is ABNORMAL, not routine; **if the supervisor schedule ever moves inside 14:15-15:30 UTC the pre-flight degrades to WARN-only and MUST be revisited** | I2 REVISE-1 bullet; section 5 [NOTE] schedule premise |
| KEPT | I1a, I1b, I5, sections 1, 2, 4; the BLOCK-2 (round-4) marker wording; I3c apart from ONE sentence | UNCHANGED -- outside the round-7 scope. Rev 3/4/5/6/7 dispositions A1-A8/B1-B3/R1-R9/N3, BLOCK-1.1/1.2/1.3, Pins 1-5, BLOCK-a/BLOCK-b, REVISE-1/REVISE-2 and NOTE-3 stand as written except where a row above amends them | I1a, I1b, I5, sections 1, 2, 4 |

**Build order:** **I1a -> I1b -> {I2 || I3 || I3c} -> I5.** I1b consumes I1a's fields; I2/I3/I3c are mutually independent once the record shape is fixed and may be built in parallel; I5 binds the
finished chain and is last.

## 7. Build-time disposition (coordinator, 2026-09-05, before the stage-B fan-out)

- **Where the counter runs.** §3.0(e) says the scorer wrapper "loops `manifest.stations`" but a shell wrapper cannot
  load the manifest without a `$PY -c` call, which collides with the stub-interpreter test idiom (round 4). Disposition:
  `score-live-trials-run.sh` (14:15) runs `structural_dead_stop.py --family-manifest "$FAMILY_MANIFEST" --output
  "$OUT/covered_listed_station_days_$STAMP.json"` ONCE, FIRST; it then loops the `stations` array of that JSON
  (`sed -nE 's/^    "([A-Z]{3,4})",?$/\1/p'` under `indent=2`, array items at indent 4) invoking the scorer per city;
  the marker is written only after the counter AND every city exited 0. `live-tally-run.sh` (14:30) no longer invokes
  the counter: it asserts the marker, then reads `count` and `fetch_start` from that same dated JSON (absent JSON ⇒
  non-zero, value-free line). One counter run per day, one file, three consumers (scorer cities, v1 numerator window,
  v1 denominator). The v1 wrapper's change list item "(3) invoke the counter" becomes "(3) read the counter JSON".
- **I3 is built as two increments on disjoint files:** I3-counter (`scripts/analysis/structural_dead_stop.py` +
  `tests/unit/test_structural_dead_stop_cli.py`) and I3-deploy (units, wrappers, deploy tests, timer-hours test); the
  deploy tests stub `$PY`, so I3-deploy does not depend on I3-counter, the leaf, or I2 being built.
- **The leaf `runtime/exec_state_db_path.py` (I2 R8/REVISE-1) is built as its own increment first**, so I2 and the
  wrappers import one shipped module. `--check` exit codes: 0 on MATCH and NO_NODE (the token tells the wrapper which),
  3 on MISMATCH and DISCOVERY_FAILED, 2 when the env var is unset/invalid; stdout is exactly one token.
