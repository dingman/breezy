> # ⚠ RESOLVED — this pooled 53% is confirmed inflated; use the corrected 12.1% instead (2026-09-02T061722Z)
>
> The challenge below was CORRECT. The per-hour diagnostic and corrected,
> hour-restricted recompute are in
> `docs/evidence/cli_basis_setup_win_rate_corrected_2026-09-02T061722Z.md`,
> pre-registered in `scripts/analysis/pre_registration_2026-09-02T061500Z.md`
> and produced by `scripts/analysis/cli_basis_hourly_profile_study.py`.
>
> Measured, not merely argued: `P(win | setup)` at hour 0 runs 87-99%
> (station-dependent) while `P(R_h == R_23)` (running max already at its
> end-of-day value) is only 1-10% there -- the "win" is overwhelmingly
> ordinary diurnal warming, not CLI-vs-ASOS basis. Both curves flatten by
> hour 16-17. Restricting to local-standard hour `>= 17` (reusing, not
> re-deriving, `cli_basis_boundary_study.STUDY_HOURS[0]`) and recomputing
> with the SAME pooling/verdict pipeline used below gives pooled
> **n=101,590, k=12,528, Wilson 95% lower=0.1213** -- a **PASS**, still
> ~1.9x the 0.06285 break-even, but roughly 4.4x smaller than the 0.5284
> this document originally reported. The correction does not kill the
> family; it does mean any sizing built on the number below would have been
> wrong by roughly that factor.
>
> The rest of this document is kept verbatim as the historical record of the
> original (inflated) measurement and the challenge that caught it. Do not
> cite the 0.5284/52% figures below for anything; cite the corrected
> document instead.

# CLI-basis candidate #2 -- archive-side `P(win | setup)` (Item 3)

Pre-registered in `scripts/analysis/pre_registration_2026-09-02T055741Z.md`.
Generated by `scripts/analysis/cli_basis_setup_win_rate_study.py`
(`.venv/bin/python scripts/analysis/cli_basis_setup_win_rate_study.py`).

**This measures ONLY whether the bet is good, never whether it is
available.** See the pre-registration's "crucial asymmetry" section. A PASS
below is necessary, not sufficient, for a GO on candidate #2; the offer-gate
scan's own unchanged `n >= 50` admissible-dense-station-day gate on
AVAILABILITY still governs whether any of this is tradeable, and it currently
stands at n=1 (UNDERPOWERED) with the one admissible day it does have
(LAX 2026-08-31) resolved LOSS -- see
`docs/evidence/cli_basis_offer_gate_settlement_2026-09-02T053500Z.md`.

## Per-station results (2021-2025 archive, margins {1, 2} pooled, all hours)

| Station | n | k | Wilson 95% lower | Wilson 95% upper |
|---|---:|---:|---:|---:|
| LAX | 87,338 | 45,441 | 0.5170 | 0.5236 |
| MDW | 87,638 | 44,088 | 0.4998 | 0.5064 |
| MIA | 86,980 | 46,779 | 0.5345 | 0.5411 |
| SFO | 86,324 | 48,310 | 0.5563 | 0.5629 |

## Pooled (4 dense stations)

n = 348,280, k = 184,618, Wilson 95% lower = 0.5284, Wilson 95% upper = 0.5317

**PASS** -- pooled `n` clears the pre-registered `n >= 100` floor by four
orders of magnitude, and the Wilson 95% lower bound (0.5284) clears the
0.06285 break-even bar by roughly 8.4x. Every individual station also clears
the bar independently (lower bounds 0.50-0.56), so the pooled PASS is not an
artifact of one outlier station.

## What this does and does not establish

- It establishes: when ASOS running-max headroom sits at 1-or-2 degrees
  below an open-tail strike, the CLI final print reaches that strike roughly
  half the time, pooled across LAX/MDW/MIA/SFO and both margins, at very high
  precision (n in the tens of thousands per station).
- It does NOT establish that the venue ever prices that tail at <= $0.05 in
  tradeable size -- that is a claim about the ORDER BOOK, not about NWS
  physics, and the archive contains no prices at all. The offer-gate scan's
  forward-tape measurement is the only source of that evidence, and it is
  unchanged by this result: n=1 admissible dense station-day, UNDERPOWERED,
  and the one event it has already resolved as a LOSS (Item 1).
- Prior strategy families (L-9) died on exactly the availability axis this
  document does not touch. A strong number here must not be read, by itself
  or later, as license to build execution logic.
