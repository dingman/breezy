# Codex strategy-lead rulings — PREREG v2 coverage and take-counting (2026-09-06)

Requested 2026-09-06 03:21 UTC after Grok Build returned 402 (balance exhausted) on the same brief; Codex ran read-only (task-mtp8vaie-2d1d4m, Codex session 01a074bb-a951-7fb3-ab44-a31c2aa28d1e). Spec citations §9 (lines 206-217) and §12 (248-258) verified by the coordinator against the spec file before filing. Output below is verbatim.

---

# Ruling: pm_us_crh_v2 coverage and take-counting

## Question A
- VERDICT: Recorder/catalog coverage alone is the registered definition; adding trade-node live/permitted/non-degraded/no-OPEN-intent uptime to “covered” would be a new post-registration denominator screen for this family.
- Operational definition: A covered listed station-day is `(station, climate_day)` with `climate_day >= 2026-09-05`, station in `{LAX, MDW, MIA, SFO}`, venue-listed/non-skip, and quote/depth capture span in `[12:00,17:00)` LST of at least 30 minutes; existing automation may exclude recorder outage overlap through resolved `QuoteTapeGap`, but must not additionally require trade-node liveness or permit state for the registered `pm_us_crh_v2` tally. If node-liveness is adopted for a successor/diagnostic rule, the exact predicate should be: recorder-covered as above AND, for the whole afternoon window, one tracked trade-node PID is alive and holds the submit-intent flock, the log shows `CurrentRungHoldStrategy subscribed`, the log has `live-trading permit issued ... expires_at_ns=...` with expiry after the window end, no permit-missing self-check, no `Trading refused:` / AMBIGUOUS create-order degradation, and the state DB has no `exec/polymarket_us/intent/current` record in `OPEN` state whose `[created_ns, retired_ns or +inf)` overlaps the window.
- Spec citations: `docs/specs/PREREG_v2_current_rung_hold_DRAFT_2026-09-04.md:206-217` defines the structural-dead pieces using window, captured quote/depth span, listed days, denominator, threshold 15, and filled Takes `= 0`; `docs/specs/PREREG_v2_current_rung_hold_DRAFT_2026-09-04.md:248-258` forbids post-hoc structural-dead additions such as extra epsilon/Wilson screens while allowing only the already-registered pin; `docs/specs/PREREG_v2_current_rung_hold_DRAFT_2026-09-04.md:262-293` records registration, D0, live provenance, and empty-store `n=0`. Supporting code/doc evidence: `docs/plans/STRUCTURAL_DEAD_RULE_2026-09-05.md:7` restates the registered wording; `scripts/analysis/structural_dead_stop.py:16-34` pins recorder/listed definitions; `scripts/analysis/structural_dead_stop.py:176-189` documents the recorder-gap residual as delaying KILL, not adding trade-node liveness. Node-predicate evidence points, if used elsewhere: `src/breezy/runtime/trade_supervisor_core.py:69-72`, `:204-211`, `:264-318`; `src/breezy/runtime/submit_intent.py:327-375`, `:435-452`; `src/breezy/adapters/polymarket_us/exec/client.py:1547-1622`, `:1799-1879`.
- Implied code/doc change: none for the registered tally; if node-liveness is desired, preregister it as a successor-family or diagnostic-only rule, not as a reinterpretation of current §9.

## Question B
- VERDICT (Case 1): (ii) A venue-accepted IOC zero-fill is a Take/consumed station-day but not a statistical trial; it enters no `n`, `k`, `S`, `I`, or PnL, and if the station-day is covered/listed it remains a zero-filled day for structural-dead purposes.
- VERDICT (Case 2): (ii) The 2026-09-05 SFO AMBIGUOUS create-order later cleared as `no-order-exists` is also not a statistical trial; after clearance it is evidence of no fill/order, so it does not defeat `filled Takes = 0`, and the covered/listed station-day still counts under Question A’s recorder definition if recorder-covered.
- Operational definition: Statistical `n` is the count of scored, admitted, filled one-contract Takes only. A zero-fill or no-order-exists unresolved/cleared Take has `dn=0`, `dk=0`, `dI=0`, `dS=0`, no scorer row, no fill-order sidecar row, and no tally-row effect. `unresolved_takes.jsonl` must not feed `family_tally_v2`; it is visibility-only until operator/reconciliation evidence resolves the submit intent.
- Spec citations: `docs/specs/PREREG_v1_current_rung_hold_2026-09-04.md:24-28` states the hypothesis over live 1-contract IOC buys and realized hold rate; `docs/specs/PREREG_v1_current_rung_hold_2026-09-04.md:41-47` says one executable station-day is the observation unit and skip-days are not Wilson zeros; `docs/specs/PREREG_v1_current_rung_hold_2026-09-04.md:57-58` defines Take as quantity 1 IOC; `docs/specs/PREREG_v1_current_rung_hold_2026-09-04.md:76-93` scores filled trials from fill price/fee and settlement; `docs/specs/PREREG_v2_current_rung_hold_DRAFT_2026-09-04.md:46-49` says v2 `held_i`, `BE_i`, and looks are over filled Takes only; `docs/specs/PREREG_v2_current_rung_hold_DRAFT_2026-09-04.md:276-280` orders looks by scored fill time; `docs/specs/PREREG_v2_current_rung_hold_DRAFT_2026-09-04.md:291-293` permits empty-store `n=0`. Existing ruling/code support: `docs/evidence/grok_partial_fill_ruling_2026-09-04.md:7-15`; `docs/evidence/grok_live_small_strategy_spec_2026-09-04.md:54-58`; `scripts/analysis/score_live_trials.py:138-144`, `:724-738`, `:1057-1059`, `:1188-1194`, `:1272-1284`.
- Implied code/doc change: none to feed `unresolved_takes.jsonl` into the tally; at most clarify rendered reports that unresolved/cleared no-fill Takes are visibility/provenance only and do not change statistical `n`.

## Cross-cutting notes (optional, <=10 lines)
Read-only observed: I did not edit files. Current `git status --short` shows unrelated modified files: `scripts/analysis/whole_tape_paper_replay.py` and `tests/unit/test_whole_tape_paper_replay.py`.
Sequential Thinking MCP was required by the repo guide but is not available in this tool session; the ruling above is therefore grounded in direct source/spec reads and line citations.


