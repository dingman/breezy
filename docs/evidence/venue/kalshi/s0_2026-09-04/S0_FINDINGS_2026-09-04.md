# S0 findings — Kalshi public-API + vendor-doc probe, 2026-09-04

Probe policy: public GETs only, `api.elections.kalshi.com/trade-api/v2`, UA-only, <=4 rps. No auth, no orders.

## Fixtures (candidates for `tests/fixtures/kalshi/`)
- `fixtures/market_KXHIGHLAX_2026-09-04.json` — `GET /markets?series_ticker=KXHIGHLAX&status=open&limit=5`
- `fixtures/orderbook_KXHIGHLAX-26SEP05-T80_2026-09-04.json` — `GET /markets/{t}/orderbook`
- `fixtures/series_KXHIGHLAX_2026-09-04.json`, `fixtures/event_KXHIGHLAX-26SEP05_2026-09-04.json`
- `fixtures/trades_KXHIGHLAX-26SEP05-T80_2026-09-04.json` — `GET /markets/trades`
- `fixtures/headers_markets.txt` — full response headers of the `/markets` GET
- `docs/*.md` — verbatim vendor markdown pulled from docs.kalshi.com (`llms.txt` index + `llms-full.txt`)

## Item table
| § | Verdict | Verbatim source + URL |
|---|---|---|
|1.6 status vocabulary|VERIFIED|market_lifecycle.md: "`initialized` … `active` … `inactive` … `closed` … `determined` … `disputed` … `amended` … `finalized`"; filter map "`unopened` → `initialized`; `open` → `active`; `paused` → `inactive`; `closed` → Any market past `close_time` that is not yet `finalized`; `settled` → `finalized`". https://docs.kalshi.com/getting_started/market_lifecycle.md. Fixture status observed = `"active"`.|
|1.7 rate limits/headers|VERIFIED (auth reads) / UNVERIFIED (unauth)|rate_limits.md: "Every **authenticated** request costs **tokens**." and "429 responses do not currently include `Retry-After` or `X-RateLimit-*` headers. There is no penalty or cooldown." https://docs.kalshi.com/getting_started/rate_limits.md. Live 200 carried NO rate-limit header (`fixtures/headers_markets.txt`). No documented tier for **unauthenticated** reads exists → the ~4 rps figure stays an empirical measurement, not a documented bound.|
|3.3 event_ticker|VERIFIED|Fixture `event_ticker: "KXHIGHLAX-26SEP05"`, market `ticker: "KXHIGHLAX-26SEP05-T80"`, `GET /events/{e}` returns `series_ticker: "KXHIGHLAX"`, `mutually_exclusive: true`, `collateral_return_type: "MECNET"`. terms.md warns: "There are occasional exceptions, so **do not parse ticker strings to infer relationships**. Best practice is to … rely on fields like `series_ticker`, `event_ticker`". https://docs.kalshi.com/getting_started/terms.md|
|3.5 tail strike operator|VERIFIED (STRICT `>` / `<`; strike is EXCLUSIVE)|T80: `strike_type:"greater"`, `floor_strike:80`, rules "is **greater than 80°** fahrenheit … resolves to Yes", `yes_sub_title:"81° or above"`. T73: `strike_type:"less"`, `cap_strike:73`, rules "is **less than 73°**", `yes_sub_title:"72° or below"`. B79.5: `strike_type:"between"`, floor 79 / cap 80, "between 79-80°" (INCLUSIVE both ends). Fixture `market_KXHIGHLAX_2026-09-04.json`.|
|3.7 close/expiration ts|VERIFIED + CONTRADICTS-PLAN|Fixture: `close_time`, `open_time`, `expected_expiration_time`, `latest_expiration_time`, `expiration_time`, `settlement_timer_seconds:300`, `can_close_early:true`, `early_close_condition`. market_lifecycle.md: "`expiration_time` | **Deprecated legacy field.** Prefer `latest_expiration_time` … use `expected_expiration_time` if you want the forecasted time." Plan §3.7 names `expiration_time` as a primary field; it is deprecated.|
|4.1 price units / bounds|CONTRADICTS-PLAN|Plan: "Prices are cents in [0.01, 0.99]". Live: prices are **fixed-point dollar STRINGS** (`yes_ask_dollars:"0.4100"`), and the market's own grid is `price_ranges:[{"start":"0.0000","end":"1.0000","step":"0.0100"}]`, `price_level_structure:"linear_cent"`. FixedPointDollars: "Most request fields accept 2-4 decimal places … responses emit up to 6." No `[0.01,0.99]` bound is published; the declared range is inclusive of 0.0000/1.0000. https://docs.kalshi.com/api-reference/orders/create-order-v2.md|
|4.2 implied prob = price|VERIFIED (venue side)|Fixture `notional_value_dollars:"1.0000"`; market_settlement.md: "**Yes outcome**: Yes contract holders receive \$1 per contract". https://docs.kalshi.com/getting_started/market_settlement.md|
|4.3 wire units of yes_bid/yes_ask|CONTRADICTS-PLAN|Integer-cent fields are GONE. llms-full.txt changelog (Jan 6, 2026): "Cent-denominated price fields will be removed from Market responses … `response_price_units`, `notional_value`, `yes_bid`, `yes_ask`, `no_bid`, `no_ask`, `last_price` … → Use `*_dollars` equivalents". Release date Jan 15, 2026. Live fixture has only `yes_bid_dollars`/`yes_ask_dollars`/`no_bid_dollars`/`no_ask_dollars` + `yes_bid_size_fp`. Any code normalizing "raw integer cents" is now dead.|
|5.2 taker θ = 0.07 + formula|UNVERIFIED|Series object gives the fee MODEL but not the coefficient: `"fee_type":"quadratic"`, `"fee_multiplier":1` (`fixtures/series_KXHIGHLAX_2026-09-04.json`). Enum (get-series-fee-changes.md): `quadratic`, `quadratic_with_maker_fees`, `quadratic_with_combo_maker_fees`, `flat`. `grep -iE "0\.07\|trading fee"` over the ENTIRE `docs.kalshi.com/llms-full.txt` (531 KB) returns nothing. The coefficient lives only in the fee schedule on `kalshi.com`, which returns **HTTP 429** to every non-browser agent (tried `/docs/kalshi-fee-schedule.pdf`, `/regulatory/fee-schedule`). Third-party blogs assert 0.07 — **not admissible** under this doc's standing rules. BLOCKER: operator must fetch the PDF in a browser, or read `fee_multiplier`/`fee_type` against one real fill.|
|5.4 rounding|CONTRADICTS-PLAN|Plan: "Kalshi rounds **UP per trade**" (implying to the cent). fee_rounding.md: "**Trade fee** | Fee from the fee model, rounded up to the nearest `$0.000001`", then "1. Compute `trade_fee = ceil_6dp(model_fee)` 2. `aligned_change = floor_precision(revenue - trade_fee)` 3. `rounding_fee = (revenue - trade_fee) - aligned_change`", plus a per-ORDER **fee accumulator** with rebates: "The fee accumulator is maintained per order across all fills". Net fee = trade fee + rounding fee - rebate. This is ceil-to-6dp + an order-level accumulator, NOT round-up-to-cent-per-trade. https://docs.kalshi.com/getting_started/fee_rounding.md|
|5.5 settlement fee|VERIFIED (zero for this product)|market_settlement.md: "Settlement fees are zero for simple yes/no determinations but may apply for sub-cent scalar settlement. The actual payout (`CollateralAmountChange`) is rounded to whole cents." KXHIGHLAX markets are `market_type:"binary"` with `result` in yes/no → zero. https://docs.kalshi.com/getting_started/market_settlement.md|
|6.1 one-book YES/NO|CONTRADICTS-PLAN — HIGHEST CONSEQUENCE|get-market-orderbook: "It returns yes bids and no bids only (no asks are returned). This is because in binary markets, a bid for yes at price X is equivalent to an ask for no at 1-X." orderbook_responses.md: "A **YES BID** at price X is equivalent to a **NO ASK** at price (\$1.00 - X)". order_direction.md: "**`bid ≡ yes`, `ask ≡ no`**, always", with the mapping table `buy/yes → outcome_side yes, book_side bid` and `buy/no → outcome_side no, book_side ask`. create-order-v2 `BookSide`: "For event markets, this refers to the **YES leg only**: `bid` means buy YES, `ask` means sell YES. (Selling YES is economically equivalent to buying NO at `1 - price`, but this endpoint quotes everything from the YES side.)" **There is ONE ticker and ONE signed position.** A YES buy and a NO buy are opposite signs of the same instrument and DO net. Plan F5's exit ("YES and NO tickers … resolve to two DISTINCT instrument ids and `OmsType.NETTING` never nets across them") is built on a false premise: Kalshi exposes no NO ticker. F5 must be rewritten as "one instrument per market ticker, signed; BUY-YES-only means `side=bid`". Live proof in fixture: yes best bid 0.4000 / no best bid 0.5900; market object `yes_ask_dollars` 0.4100 = 1 − 0.5900 and `no_ask_dollars` 0.6000 = 1 − 0.4000.|
|6.2 depth beyond top of book|VERIFIED (available)|`fixtures/orderbook_...json` returned 30 yes levels and 36 no levels with sizes, e.g. `["0.0100","6677.00"]`. orderbook_responses.md: "Arrays are sorted by price in **ascending order** … the **highest** bid (best bid) is the **last** element". Size field is `count_fp` (2-dp string). The 60 s sampling campaign is still to be run.|
|6.3 tick size|VERIFIED + CONTRADICTS-PLAN|There is **no `tick_size` field**. llms-full.txt (Apr 30, 2026): "The deprecated `tick_size` field on Market response objects has been deprecated since **Jan 5, 2026** and will be removed on **May 7, 2026**. Use `price_level_structure` and `price_ranges[].step` to determine each market's valid tick sizes." Fixture: `price_level_structure:"linear_cent"`, `price_ranges:[{start "0.0000", end "1.0000", step "0.0100"}]`. Tapered structures exist (`center_{c}_edge_{e}_cent`, shorthands `whole`=$0.01 … `centi`=$0.0001) so tick MUST be read per-market, never assumed 1c.|
|6.4 min size / notional|PARTIAL VERIFIED (size) / UNVERIFIED (notional)|fixed_point_migration.md: "Accept 0-2 decimal places on input … **Minimum granularity is 0.01 contracts**"; FixedPointCount: "Fractional contract values (e.g., \"2.50\") are supported; the minimum granularity is 0.01 contracts." No minimum-notional and no maximum-order-size figure appears anywhere in `llms-full.txt`. Confirm at go-live by rejection.|
|6.5 TIF / IOC / partial fills / client_order_id / cancel|VERIFIED + CONTRADICTS-PLAN (endpoint + vocabulary)|**Endpoint is `POST /portfolio/events/orders`**, not `POST /portfolio/orders`: "The legacy `/portfolio/orders` endpoint will be deprecated no earlier than May 6, 2026 — clients should migrate to this path." Required body: `ticker, side, count, price, time_in_force, self_trade_prevention_type`. **No `action`/`type` fields** in V2; `side` is `BookSide` enum `bid`\|`ask` (not `yes`/`no`). `action`/`side`(yes/no) are legacy, deprecated, "will not be removed before May 28, 2026" (order_direction.md). **IOC EXISTS**: `time_in_force` enum = `fill_or_kill`, `good_till_canceled`, `immediate_or_cancel` — "`GTT` is an internal execution type and is not a valid API value"; "`immediate_or_cancel` … cannot be combined with `expiration_time`". `expiration_time` = "Optional Unix timestamp in **seconds**" (NOT `expiration_ts`), used with `good_till_canceled`. **Partial fills**: response carries `fill_count`, `remaining_count` ("For IOC orders, this reflects the final state after unfilled contracts are canceled"), `average_fill_price`, `average_fee_paid`, `ts_ms`. **`client_order_id` exists** (request + echoed on response and on cancel). Other flags: `post_only`, `reduce_only`, `cancel_order_on_pause`, `order_group_id`, `subaccount`, `exchange_index`; `self_trade_prevention_type` REQUIRED, enum `taker_at_cross`\|`maker`. **Cancel**: `DELETE /portfolio/events/orders/{order_id}` (returns `{order_id, client_order_id, reduced_by}`) — plan's `DELETE /portfolio/orders/{id}` is the legacy path. **Batch cancel**: `DELETE /portfolio/events/orders/batched`. **Cancel-all**: `DELETE /portfolio/events/orders`; "Newly placed orders may also be cancelled during the minute after the request." **Open orders read**: `GET /portfolio/orders`, filter `status` = "resting, canceled, or executed". **Fill read**: `GET /portfolio/fills`. URLs: https://docs.kalshi.com/api-reference/orders/create-order-v2.md , /cancel-order-v2.md , /batch-cancel-orders-v2.md , /cancel-all-orders.md , /get-orders.md , https://docs.kalshi.com/api-reference/portfolio/get-fills.md , https://docs.kalshi.com/getting_started/order_direction.md|
|6.6 public trade tape|VERIFIED|`GET /markets/trades?ticker=…` returned rows with `trade_id, created_time, count_fp, yes_price_dollars, no_price_dollars, taker_side, taker_outcome_side, taker_book_side, is_block_trade`. Fixture `fixtures/trades_KXHIGHLAX-26SEP05-T80_2026-09-04.json`. `trade_execution=False` can now expire on Kalshi.|
|7.1 auth headers|VERIFIED|api_keys.md: "`KALSHI-ACCESS-KEY` - the Key ID", "`KALSHI-ACCESS-TIMESTAMP` - the request timestamp in **ms**", "`KALSHI-ACCESS-SIGNATURE` - request hash signed with private key". https://docs.kalshi.com/getting_started/api_keys.md|
|7.2 canonical string|VERIFIED (with a trap)|api_keys.md: "The above signature is generated by signing a concatenation of the timestamp, the HTTP method and the path." Code: `msg_string = timestampt_str + method + path_without_query`. TRAP: "When signing requests, use the path **without query parameters** … sign only `/trade-api/v2/portfolio/orders` (strip the `?` and everything after it)" — and the path signed is the **full path from the API root including `/trade-api/v2`**. https://docs.kalshi.com/getting_started/quick_start_authenticated_requests.md|
|7.3 RSA-PSS SHA256|VERIFIED (incl. salt length)|api_keys.md `sign_pss_text`: `padding.PSS(mgf=padding.MGF1(hashes.SHA256()), salt_length=padding.PSS.DIGEST_LENGTH), hashes.SHA256()`, output `base64.b64encode(...)`. JS twin: `RSA_PKCS1_PSS_PADDING`, `RSA_PSS_SALTLEN_DIGEST`, `RSA-SHA256`. **Salt length = digest length (32)**, base64 signature. Not Ed25519 — `signing.py` cannot be reused beyond `sign(bytes)->bytes`.|
|10.4 settlement timing|PARTIAL VERIFIED + **CONTRADICTS-PLAN ON THE SETTLEMENT SOURCE**|Fixture `settlement_timer_seconds:300`; market_lifecycle.md: "A settlement timer then runs for `settlement_timer_seconds` … During this window the market remains at `determined` and the result may be disputed." Product schedule verbatim from `early_close_condition`: "The Last Trading Time will be 11:59 PM local time on September 05, 2026 … Expiration will occur on the sooner of the first **7:00 or 8:00 AM ET** following the release of the data for September 05, 2026, or one week after September 05, 2026." **BUT the settlement source is NOT the NWS CLI product**: `rules_primary` = "If the maximum temperature recorded at Los Angeles (CLILAX) … according to **The Weather Company**, then the market resolves to Yes", `settlement_sources:[{"name":"The Weather Company","url":"https://weather.com/kalshi"}]`, and `rules_secondary` explicitly warns "the official and final value used to determine this market is the maximum/minimum temperature as reported by the Weather Company." The plan's premise that a Kalshi station-day and a Polymarket.us station-day "settle on **one** NWS CLI event" is **false for Kalshi**; Kalshi settles on TWC, with CLILAX named only as the station identifier. This is a live divergence risk for S8's "settlement-truth reconciliation per station (R4)" and it strengthens (not weakens) the no-pooling ruling. The 20-settlement observation campaign is still to be run.|
|14.1 eligibility / KYC / state restrictions|UNVERIFIED|`docs.kalshi.com` (whole `llms.txt` + `llms-full.txt`) contains no eligibility, jurisdiction, KYC or state-restriction text. `kalshi.com` and `kalshi.com/regulatory/*` return **HTTP 429** to every non-browser UA tried, so no vendor text could be quoted. Only in-repo-adjacent verified fact remains the series object: `additional_prohibitions: ["Persons who are employed by any of the Source Agencies are not permitted to trade on the Contract.", "Persons who hold any material, non-public information on the Underlying are not permitted to trade on the Contract."]` (`fixtures/series_KXHIGHLAX_2026-09-04.json`). Operator decision, per plan.|

## Field-name crib for the fixtures
tick: **no `tick_size`** → `price_level_structure` + `price_ranges[].step`. Quotes: `yes_bid_dollars`/`yes_ask_dollars`/`no_bid_dollars`/`no_ask_dollars` (+ `yes_bid_size_fp`/`yes_ask_size_fp`); **no** `yes_bid`/`yes_ask` cent fields. Timestamps: `open_time`, `close_time`, `expected_expiration_time`, `latest_expiration_time`, `expiration_time` (deprecated), `settlement_timer_seconds`, `can_close_early`, `early_close_condition`, `occurrence_datetime`. Rules: `rules_primary`, `rules_secondary`, `title`, `yes_sub_title`, `no_sub_title`. Strikes: `strike_type` (`greater`/`less`/`between`), `floor_strike`, `cap_strike`. Fees: **none on the market object** — `fee_type` + `fee_multiplier` live on the SERIES object.
