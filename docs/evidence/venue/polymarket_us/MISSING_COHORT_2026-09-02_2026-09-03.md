# Missing 2026-09-02 station-day cohort — VENUE NEVER LISTED IT (2026-09-03)

**Symptom.** The 09-03 13:30Z M_A and M_B runs carry station-day rows for
08-30, 08-31, 09-01 and 09-03 and ZERO rows for 2026-09-02, all four stations.

**Verdict: VENUE-NEVER-LISTED.** Not a capture, ingest, join, or discovery
defect. Evidence, in order of the decision tree:

1. **Recorder journal** (`journalctl --user -u breezy-quote-tape`, 08-30 →
   09-03 12:00Z): the string `2026-09-02-` (slug-date with trailing hyphen)
   appears in **0** lines — never subscribed, unsubscribed, rejected, skipped
   or refused. `record-drop` appears 0 times. Note `Loaded N instruments` is the
   provider's cumulative cache count; the discovery result is the adjacent
   `POLYMARKET_US-discovery: ... loaded N active market(s)` line.
2. **Cohort timeline.** pid 3459201 held the 09-01 cohort until it closed
   (`endDate 2026-09-02T06:00:00Z`; unsubscribes as `discovery-missing`
   06:00–08:16Z). 09:00–09:14Z: 15 cycles raised `VenuePayloadError: returned
   zero configured-city weather markets` — the zero-universe guard
   (`provider.py:326-332`) firing CORRECTLY on a genuinely empty universe.
   pid 670812 at **10:09:41Z subscribed the 2026-09-03 cohort** (initial, 30),
   and every later reload (10:56, 16:56, 22:56, 04:56, 09:00) carried 09-03
   with `subscribed=() unsubscribed=() blocked_missing_cache=()`.
3. **Venue catalog, unfiltered** (closed- and archived-inclusive):
   `GET https://gateway.polymarket.us/v1/markets?limit=100&offset=N&orderBy=endDate&orderDirection=asc&categories=climate`
   → 3713 markets 2026-04-22 → 2026-09-04; the last page's slug-date histogram
   is `{08-31: 23, 09-01: 30, 09-03: 30, 09-04: 17}`. No 09-02 in any status.
4. **By-slug positive control** (`/v1/market/slug/{slug}`):
   `tc-temp-mdwhigh-2026-09-02-gte91lt92f`, `tc-temp-nychigh-2026-09-02-gte83lt84f`,
   `tc-temp-nychigh-2026-09-02-lt82f` → **404**;
   `tc-temp-mdwhigh-2026-09-01-gte91lt92f` → 200 (`id 596124`, RESOLVED,
   `createdAt 2026-08-31T10:30:53Z`);
   `tc-temp-nychigh-2026-09-03-gte83lt84f` → 200 (`id 614720`, OPEN,
   `createdAt 2026-09-02T09:45:31Z`).
5. **No exclusion path in code.** Filters live only in `provider.py:506-513`
   and were removed in the probes; `MAX_DISCOVERY_PAGES=50 × limit=100 = 5000
   ≥ 3713`; the city gate (`provider.py:220-226`) and dedupe (`:335-336`) RAISE
   rather than skip; the sole silent `continue` (`:207-209`) cannot match a
   well-formed `tc-temp-<city>high-` slug.

**Venue behaviour, measured.** Cohorts are created ~T-2 days at ~09:45–10:30Z.
Days with NO cohort across the whole catalog: **04-24…04-28, 06-25, 06-26,
06-27, 07-08, 07-12, 08-28, 09-02**; partial days 07-09 (29) and 08-06 (24).
Roughly 12 of ~135 listed days (~9%) are absent. This is recurring venue
behaviour, not an incident.

**Consequences.**
- The M_B kill/survive clock (n≥60 / n≥150 at ~3 taken/day) must be lengthened
  by the venue's ~9% skip rate; the 09-22 / 10-21 estimates are optimistic by
  about a week each. No engineering shortens this.
- The zero-universe refusal is correct and stays; it is the only thing that
  distinguishes "venue listed nothing" from "we lost the feed" (L-8).
- The L-17 `updatedAt` aborts earlier on 09-02 were a concurrent, separate
  fault (fixed) and did not cause this gap.
- A first read of the recorder instance `config.json`s claimed
  `instrument_reload_interval_mins: null` meant discovery ran once per process.
  The journal refutes this: the reload loop runs 6-hourly and retries every
  60 s after a failure. **Discarded** — a config field read from a snapshot
  is not the effective runtime value (cf. `quote_tape_cli.py:263`).

**Stale documentation flagged.** `VENUE_FACTS_2026-08-25.md` Q7's traversal
`api.polymarket.us/v1/events?seriesId=…` now returns
`401 Missing required API key headers` unauthenticated; the working public
read path is `gateway.polymarket.us` (`config.py:108`
`POLYMARKET_US_GATEWAY_BASE_URL`).
