# LADDER_EV design — peer-review gate record (2026-09-07)

Scope: the planning-gate record for `docs/strategies/breezy_strategy_ladder_ev_2026-09-07.md`
(design v2). Not a trial, not a verdict on edge, not an enablement. Tree `ccf9161`.

## Stage 1 — evidence (four read-only investigations, coordinator spot-checked)
Seams: A existing decision pipeline; B binding constraints / venue facts / PREREG; C data
assets and backtest infrastructure; D Nautilus 1.231.0 native capabilities (installed
source, positive control 4 hits). Every load-bearing citation below was re-read by the
coordinator at the cited line before it entered the sheet.

## §A — Existing decision pipeline (code)
- A1. LIVE wiring = `current_rung_hold` only (`src/breezy/app/trade.py:41-44`): one Strategy instance per station (`composition.py:204-208`), subscribes all of today's HIGH-ladder instrument ids (`composition.py:116-132`, instrument set frozen at 16:50Z compose, GL-8), first executable QuoteTick in [12:00,17:00) LST is THE trial; no ranking, latch consumes the station-day (`strategy.py:386-387,467-474`).
- A2. CRH fair-p = frozen Wilson-95% lower `P_HOLD_LOWER[(station,season,hour_lst,width,m)]` from the CLI archive (`archive_table.py`; `decision.py:293-296`), keyed by running max from `StationObservation`. Selector only, not a density (L-21).
- A3. CRH edge test: `p_hold_lower > ask + θ·ask·(1−ask)` (`decision.py:298-300`, θ=0.06 `config.py:226`); executable iff 0.05<ask<0.95 and displayed size ≥1 (`decision.py:286-291`); Take.limit_price = displayed ask, qty=1, IOC LIMIT BUY (`decision.py:302-304`, `strategy.py:542-548`). Top-of-book QuoteTick only; no depth walk, no tick floor, no slippage, no quote-age check (now_ns = tick.ts_event). Obs stale 50 min. Hold-to-settlement, `entry_only_halt=True`; no exit policy (T-9 open).
- A4. Forecast family (`forecast_mispricing`, `forecast_revision`, `calibration_mean_reversion`): full decision + IOC/MARKET submit code, backtest-capable, NO live composition, programme-KILLED. Fair-p = `WeatherProbabilityEngine.bucket_probability(facts, expected_high_f, issuance_lead)`: CDF of N(forecast+bias, σ) over the closed bucket (`weather_common/probability.py:328-371`); σ(h) = max(0.25, sqrt(σ_floor² + (σ_per_sqrt_hour·√h)²)) capped (`probability.py:245-248`; defaults `default_conus_summer_error_model` :419-445). `fit_error_model(records, min_samples=40, train_end_exclusive=…)` fits bias/σ by station/month/horizon with train-window leakage refusal (`calibration.py:60-124`) — not wired into any driver (NOT ESTABLISHED). Costs = flat `transaction_cost_prob=0.015` (not the venue formula); `min_entry_edge=0.06`, `exit_edge=0.015`, `min_model_edge=0.04`; sizing affine in edge, clip [1,150]; `RiskManager` caps max_position=250, max_simultaneous=12, max_equity_fraction=0.08, `exclusive_bucket_conflict` blocks a second long-YES on the same event (`risk.py:513-526,638-704`). Requires an injected `ForecastSource.snapshot(station, climate_day, now)` (`forecast_source.py:103-118`) — none exists in production.
- A5. Lock family (`running_extreme_lock`, `cli_settlement_print_lock`): REFUTED ×3 but coded; print-lock has the best cost machinery: depth-aware VWAP walk, `venue_fee_prob + slippage_prob`, marketable IOC LIMIT at ask+slippage, dollar-cost-basis clip (`decision.py:428-531`).
- A6. Shared primitives (reuse): `venue_fee_prob(executable_price, fee_coefficient)` = θ·p·(1−p) (`costs.py:149-177`); `trade_cost_prob(…, slippage_prob)` (:180-210); `depth_aware_trade_cost_prob(ask_levels, quantity, price_scale, fee_coefficient, slippage_floor_prob) -> DepthAwareTradeCost` VWAP walk, slippage = max(floor, VWAP−TOB) (:261-342); `fee_coefficient_from_info`; `FeeCoefficientSource` (must raise, never default); `edge_after_costs(model_p, bid_p, ask_p, intent_long_yes, cost)` (`risk.py:732-751`); `RiskManager.quote_tradable` (spread/liquidity/stale) (`risk.py:467-502`); `walk_ask_ladder`/`ask_levels`/`available_ask_depth`/`levels_within_price` (`ladder.py:135-224`); `market_quote_from_depth(depth, include_ask_ladder=False default)` (`depth10.py:44-74`); `MispricingContract` (`bucket_contract.py:36-95`); `observed_equity(cache, portfolio, ids)` fail-closed (`equity.py:80-116`).
- A7. Scoring chain: `ScoredTrial`/`score_trial` (held = bucket.contains(settlement_tmax_f); pnl = 1[held] − fill_px − fee) (`trial_scorer.py:119-208`); `write_scored_trials`/`read_scored_trials` (`scored_trial_store.py`); v2 family score S=Σ(held−BE)/√I (`current_rung_hold_v2.py:48-59`); offline `scripts/analysis/family_tally_v2.py`.
- A8. Operator/enablement: `DailySpendLedger.authorize_order_cost(price_usd, quantity, now_ns)` books price×qty ceil-cent against env caps, no defaults, called from the exec client `_submit_order` not from strategies (`operator_controls.py:301-380`; `exec/client.py:1569`); `OrderSubmissionPermit.issue` is the live submit capability (`order_enablement.py`). Live body = BUY / IOC / LIMIT / qty=1 / outcomeSide=YES only (`submit_chain.py:225-250`). NO-side via outcomeSide=NO + price inversion is documented in `backtest_order_guard.py:115-127` but NOT implemented in submit_chain.
- A9. ABSENT in code: cross-market ranking; a shared depth-aware EV primitive used by anything live (CRH uses displayed ask); Kelly/fractional sizing (grep kelly = 0 in src/breezy); exit policy for CRH/locks; post-fill invalidation for CRH.

## §B — Binding constraints, venue facts, microstructure, PREREG
- B1. DEAD/FORBIDDEN (do not redesign): lock family ×3 — the near-certain rung is never offered (L-9 `LESSONS.md:449-473`; print-lock 0 asks/3332 rows; H4 condition 100%/ask 0%; H1/H2 0/4); L-9 amendment: post-peak 0.01 book is a lottery already lost (adjacent rungs 100% offered at 0.01–0.03, winner unoffered); forecast family KILLED under honest σ≈2.8 °F at 24 h (edge at ask 0.85 fell 0.175→0.029 <0.06; no forecast ingest existed); K1 cheap-open DEAD on Kalshi 2023+ exhaustive (≤0.02 n=456 k=2; ≤0.03 n=1002 k=12; ≤0.05 n=2317 k=35; all 5 stations) and PM tape measured population 0; H5 sticky-ladder = same object, killed via K1; G-01 interior prelim→final revision FAIL MDW 13.96%/NYC 11.79%/SFO 4.50% (n≈1800 each; LAX/MIA pass; open tails survive, downward revision 0.21%); L-21 climatology (unconditional archive p) is NOT the comparator for an information-conditioned ask; H2 lower tail rejected (97% of prelim→final changes are UP); long-only maker rejected (post-lock winner is a 0.99 bid queue); NYC excluded from running-max families (hourly obs cadence, L-13); "fourth lock" forbidden by standing verdict.
- B2. LIVE/THIN/UNTESTED: M_B current-rung-hold v2 is the registered live family (D0=2026-09-05, lags 30/45, NYC out; fill 0, n=0, one SFO 09-05 take → AMBIGUOUS, retired); M_A pre-lock winner-ask n=4 UNDERPOWERED (09-01 MDW winner asks min 0.21 in 9472 cells, SFO min 0.65) — afternoon winner asks in (0.05,0.95) DO sometimes exist pre-lock; CLI-basis boundary tail P(win|setup,h≥17) Wilson-lower 0.1213 (n=101,590) ≈1.9× BE at 5¢ but offered-population adverse selection unmeasured (needs ~245 offered settlements); lagged_anomaly_tail design-only never run.
- B3. VENUE: fee = θ·C·p·(1−p), taker θ=0.06, maker −0.0125, banker's rounding to $0.01, NO minimum fee (1 contract @0.01 → $0.00; max $0.02 at p=0.5) (`VENUE_FACTS_2026-08-25.md:91-111`; `OQ8_MINIMUM_FEE_2026-09-04.md:6-34`); tick 0.01 on 3743/3743 weather markets; minimumTradeQty 0.01 since 2026-06-14 (1 before; decimal acceptance live UNRESOLVED); order types LIMIT/MARKET; TIF DAY/GTC/GTD/IOC/FOK (live acceptance per TIF NOT ESTABLISHED beyond IOC/GTC); ladder = 6 rungs per city-day: `lt`, four 2 °F inclusive interiors (A ≤ tmax ≤ B), `gte`; surface = 5 cities (LAX, MDW, MIA, NYC, SFO) × daily HIGH only, nothing else; open universe = 5 × 2 climate days × 6 = 60 markets; listing D−1 ≈09:45Z, gameStart D 05:00Z, end D+1 05:00Z; ~9% of station-days never listed; settlement = NWS CLI daily, 08:00 ET next day (METAR conflict → 11:00 ET; 7-day fallback); corrections 0.14% in archive; price history FORWARD-ONLY (no public trade tape, expired markets keep only settlementPx); NO is the other side of the SAME book (no NO instrument).
- B4. EXECUTION SEMANTICS: IOC zero-fill is ACCEPTED_ZERO_FILL_TERMINAL only with explicit terminal status AND cumQuantity==0; anything else (5xx, transport, 4xx-with-id, 200+id+non-terminal) is AMBIGUOUS → account-wide submit-intent latch OPEN until operator clears; one AMBIGUOUS burns the rest of the day (GL-4). Zero-fill takes are NOT trials. Fill better than displayed ask is a defect signature (L-25). Nautilus cannot cancel an INITIALIZED order.
- B5. MICROSTRUCTURE (provisional, selection-biased 08-31 tape): median exit-side depth 0.30 contracts (bid side bimodal: some 143/1431, three at exactly 0.30); median spread 16% of ask; ask-size percentiles p10 6.3 / p25 25.0 / p50 43.1 / p75 155.7; 57% of $24.53 notionals exceed level-0, p90 VWAP walk 0.137 (BL-25); `slippage_prob` UNMEASURED (1-tick placeholder); Depth10 captured (10 levels/side, truncation flagged).
- B6. PREREG (binding for any NEW family): register BEFORE first fill; any parameter/table/threshold change = new family, n resets, never pool; trial = filled q=1 Take only (not skip-days, zero-fills, AMBIGUOUS, qty≠1, paper/shadow/archive/Kalshi); v2 statistic per-row S_k=Σ(held_i−BE_i)/√ΣBE_i(1−BE_i), I_max=40, two one-sided α=0.025, Lan-DeMets O'Brien-Fleming, looks every 10 filled takes n=10..160; KILL on S_k ≤ b^fut or cell_dead or ΣPnL ≤ −60 or structural-dead (0 fills on ≥15 afternoon-covered listed station-days); SURVIVE needs S_k ≥ b^eff AND ΣPnL>0 AND no cell_dead; D0+165 terminal. Operator-only: max daily budget, max per position (names in operator.env, values never assigned build-side), enablement/permit, positive control, Kalshi account.
- B7. BUDGET/CLOCK: G-02 ROI NO-GO (~$3/$9/$15 per day net per 100 contracts, capital $300–500) still standing beside the live-small deployment (contradiction acknowledged, later-dated = live); balance 97.91; qty=1; clock 0.9–1.6 trials/day → n=60 in 38–66 days; observed fills 0/day.
- B8. OPEN CONTRADICTIONS: "median bid 0.3" is an average of two populations; L-9 "never offered" was measured post-print/post-peak, M_A shows pre-lock winner asks exist (underpowered); Kalshi settlement source reconciliation still open (S8).

## §C — Data assets and backtest infrastructure
- C1. QUOTE TAPE: live Arrow-IPC feather under `~/.local/share/breezy/catalog/quote_tape/polymarket_us/live/<instance>/` — 18 instances, 17 GB; converted parquet `data/` 236 MB. Per WS frame: QuoteTick (required) + OrderBookDepth10 (10 levels/side) + InstrumentStatus/MarkPriceUpdate/InstrumentClose + custom QuoteTapeGap/DepthTruncation/VenueClockOffset/VenueSettlementSnapshot (`adapters/polymarket_us/tape_records.py`). Cadence = WS push. Converted quote_tick instrument-days: 08-30 (5), 08-31 (2), 09-01 (30), 09-03 (29), 09-04 (27), 09-05 (28), 09-06 (30); 09-02 = 0 (L-20: `data/` measures converted tape, not captured `live/`). Instance 5a111bca (1.2 GB) holds all 09-01 afternoon quotes.
- C2. NWS CLI (live): per-(venue,city) catalogs `~/.local/share/breezy/catalog/polymarket_us/{NYC,SFO,MIA,MDW,LAX}/data/custom_nws_climate_day/`, climate days from 2026-08-16/17, ~19 days per station, finals 18–19 each; fields tmax/tmin/tavg + flags, is_final, correction_flag, revision_seq, is_superseded, issuance_time_ns, retrieved_at_ns; poll 300 s. `NwsClimateDay.ts_init = retrieved_at_ns`; ts_event = climate-day end (final) or issuance (prelim). Finals land 5.3–8.0 h before the 14:15Z scorer tick.
- C3. ARCHIVED CLI (IEM AFOS): settlement-alignment cache 308 MB; `settlement_truth.csv` 9,185 labelled station-days, 2020-12-19..2026-08-24 (11 missing days, 478 multi-final, 29 prelim-only). ts_init = issuance. Not on-disk as Nautilus parquet.
- C4. INTRADAY OBS: `StationObservation` (ts_event=observed_at, ts_init=received_at, receipt strictly after observed) — live actor publishes and DROPS; NO parquet on disk. Replay synthesizes receipt = observed + lag (30/45 min arms) from IEM ASOS CSV. Measured source lag: IEM 19–43 min (MDW), NWS API 5-min integer °C ~21 min (KMDW), NYC hourly only (~45 min).
- C5. FORECASTS: NOT INGESTED. `docs/plans/forecast_ingest_2026-09-01.md` is a PLAN (Open-Meteo previous-runs vintage layer). `ForecastSnapshot` is a plain dataclass (not Nautilus Data), `ForecastSource` a Protocol with no production implementation. Open-Meteo previous-runs archive: hourly `temperature_2m_previous_dayN`, valid-time anchored, contiguous 2022-01..2023-12 + present; 2024-01 empty; ECMWF/ICON 0 rows at 2022-01-01. No ensemble/uncertainty field anywhere. NWS point forecast probed only.
- C6. BACKTEST HARNESS (`runtime/backtest_harness.py:663-794`; `docs/specs/BACKTEST_VENUE_CONFIG.md`): venue POLYMARKET_US, NETTING, CASH USD, book L2_MBP, `FillModel()`, `PolymarketUSFeeModel()`, latency None, `liquidity_consumption=True`, trade/bar/queue False, settlement_prices {id: 0.0|1.0} required. Under L2, QuoteTick does NOT mutate the book — fills come only from OrderBookDepth10 (`QuoteOnlyReplayError`); IOC remainder cancelled; price improvement refused (`ImpossibleFillPriceError`); `prob_slippage` inert under L2. `BacktestOrderGuard` refuses post_only and any naked SELL (incl. reduce_only); settlement legs never publish OrderInitialized. Weather data via separate `add_data(client_id=NWS_BACKTEST_CLIENT_ID)`.
- C7. REPLAY CLOCK: Nautilus sorts and advances on `ts_init` ONLY (`NT/backtest/engine.pyx:903`). Availability-time correctness is therefore entirely whoever sets ts_init: live CLI = retrieval, archived CLI = issuance, obs = receipt, forecast = nothing yet (published_at exists only on the in-memory dataclass).
- C8. PAPER/WHOLE-TAPE REPLAY: converts one live instance → work catalog; scores vs highest-revision FINAL CLI (LOOK_AHEAD_CAVEAT: post-decision corrections are used as truth); classifications CLEAN/EMPTY/LIVE/CORRUPT/PREFLIGHT_ERROR, blocked buckets EMPTY_WINDOW/NO_FINAL_SETTLEMENT/UNSUPPORTED_STATION; last report: 12 CLEAN, 14 unique-winner station-days replayed × lags 30/45, take 12/14, header "MECHANISM TEST — NO VERDICT". Driver ~0.5 GB/min before the L-31 fix (per-definition-row reselect); current cost NOT ESTABLISHED by the investigator (coordinator memory says n=1 ≈ 80 s / 674 MB after fix — treat as unverified).
- C9. JOINT-COVERAGE VERDICT: quote+CLI-settlement backtest possible from 2026-08-30 (7 calendar dates converted, ~151 instrument-days, 0 on 09-02); quote+forecast+settlement = ZERO station-days today; forecast archive 2022–2023 can CALIBRATE forecast error against CLI labels but cannot backtest prices (forward-only; uncaptured hours are permanently lost). Slippage modelling is bounded by depth coverage (converted depth 80 MB vs 17 GB live).

## §D — Nautilus 1.231.0 native capabilities (installed source verified; positive control 4 hits)
- D1. NATIVE: one `Strategy(Actor)` subscribes N books (`subscribe_order_book_deltas/_depth/_at_interval`, one OrderBook per InstrumentId in cache; `cache.order_book(id)`); `OrderBook.best_bid/ask_price/size`, `spread()`, `midpoint()` (None if side empty); depth walk `get_avg_px_for_quantity(qty, side)` (0.0 if none), `get_worst_px_for_quantity`, `get_quantity_for_price(price, side)`, `get_quantity_at_level`, `simulate_fills(order, price_prec, size_prec, is_aggressive) -> list[(Price, Quantity)]`, `get_all_crossed_levels` (`model/book.pxd:63-73`).
- D2. NATIVE backtest realism: L2/L3 matching engine applies OrderBookDeltas and walks depth for MARKET/aggressive fills (`backtest/engine.pyx:4384-4420, 6225-6343, 6554-6588`); IOC leftover cancelled; L1 venue ignores deltas and slips 1 tick when exhausted (trap); `post_only` rejected if it would take. `FillModel(prob_fill_on_limit, prob_slippage, random_seed)` + `BestPriceFillModel`, `OneTickSlippageFillModel`, `TwoTierFillModel`, `ProbabilisticFillModel`, `SizeAwareFillModel`; `LatencyModel(base/insert/update/cancel_latency_nanos)`; `FeeModel.get_commission(order, fill_qty, fill_px, instrument) -> Money` (post-fill; NOT a pre-trade probability-cost) with `MakerTakerFeeModel`/`FixedFeeModel`/`PerContractFeeModel`; `BacktestVenueConfig(book_type, fill_model, latency_model, fee_model, liquidity_consumption=False default, support_gtd_orders, use_reduce_only, bar_execution)`.
- D3. PARTIAL sizing: `nautilus_trader.trading.sizing` does not exist; `risk/sizing.pyx` `FixedRiskSizer.calculate(entry, stop_loss, equity, risk, …)` is stop-distance based (wrong unit for a 0/1 premium-at-risk binary); Kelly = 0 hits.
- D4. PARTIAL risk: `RiskEngineConfig` = bypass, max_order_submit_rate, max_order_modify_rate, max_notional_per_order{}, debug — nothing else; fail-open without account/empty dict/MARKET (already ruled F-1..F-4); `net_exposure` unit is premium not payout (L-2). Queries available as INPUTS: `cache.positions_open[_count]`, `orders_open[_count]` (filterable by venue/instrument/strategy/side), `portfolio.net_exposure(s)`, `unrealized_pnl(s)`, `balances_locked`. No native max-open-positions or daily-budget GATE.
- D5. NATIVE orders: `LimitOrder(time_in_force ∈ GTC/IOC/FOK/GTD/DAY, expire_time_ns, post_only, reduce_only, contingency_type OCO/OTO/OUO, order_list_id, exec_algorithm_id/params)`; `OrderList`; `ExecAlgorithm(Actor)`; example `TWAPExecAlgorithm` spawns MARKET children (not suitable as-is).
- D6. NATIVE scheduling: `clock.set_timer(name, interval, start_time, stop_time, callback, allow_past, fire_immediately)` and `set_time_alert(...)`; Actor routes to `on_event` only when RUNNING; L-16: a raise inside a LiveClock timer callback is silently discarded — callbacks must catch, log, and latch.
- D7. NATIVE data: `@customdataclass` (Breezy uses hand-written Data + register_arrow instead), `ParquetDataCatalog.write_data/query` (non-Nautilus types wrapped in CustomData), `BacktestDataConfig(catalog_path, data_cls, start_time, end_time, client_id, metadata)`; replay key = ts_init (D/C7). Skill traps (silent same-range rewrite, metadata topic mismatch) bind.
- D8. PARTIAL analytics: `publish_signal(name, value ∈ int|float|str)`/`subscribe_signal`; `publish_data(DataType, Data)`; `MessageBus.publish/subscribe` with wildcards; `Cache.add(key, bytes)/get` untyped KV; `PortfolioAnalyzer` post-hoc stats (Sharpe, WinRate, Expectancy, MaxDrawdown). No per-tick ranker, no opportunity-table type, no EV statistic (grep ranker/opportunity = 0). `GreeksCalculator` applicability to BinaryOption NOT ESTABLISHED.
- D9. GENUINE GAPS (bespoke is justified): ranker math (model p vs executable price, EV after venue fee, cross-rung sort); pre-trade probability-cost (FeeModel is post-fill); premium-at-risk / fractional-Kelly sizer; max-open-positions and daily-budget GATE (Breezy's `DailySpendLedger` already fills the budget gate at the exec client); an opportunity-row Data type if rows must be catalogued for replay/audit.

## Stage 2 — design v1 (Grok, read-only), Stage 3 — three independent adversarial reviews

All three: **ACCEPT-WITH-CHANGES**. Reconciled change list C1–C16 (applied in v2 §14):

| Id | Source | Finding (compressed) |
|---|---|---|
| C1 | architect, CRITICAL | A Strategy cannot issue venue GETs; discovery is the DataClient's (`provider.py:323-496`, `data.py:1048-1096`); rediscover = `cache.instruments` diff |
| C2 | architect, HIGH | Cross-family collision with live CRH: separate `SharedExposureView`s, account-wide AMBIGUOUS latch → shadow/replay only while CRH live |
| C3 | math CRITICAL + codex MEDIUM | Margin never reached the 0.06 that killed the forecast family; slippage floor double-counted → one cash cost C, margin 0.02→0.06 over 6–24 h |
| C4 | math, HIGH | Cheap-ask exclusion must be time-independent (K1 is a price-level fact); `lt` rung refused for entry unconditionally |
| C5 | math, HIGH | Exit rule double-counted the sunk entry fee |
| C6 | math + codex | Split scored universe (60) from DEGRADED entry universe (4 stations, D only, afternoon window) |
| C7 | codex, HIGH | Shipped `ask_levels` falls back to top-of-book (`ladder.py:185-198`) → fail-closed depth adapter |
| C8 | codex, HIGH | `latency_model=None` optimistic; fill only against the next Depth10 after decision + latency; sensitivity arms |
| C9 | math, MEDIUM | `ev_net/p_exec` ranking favours lottery rungs → rank by absolute EV |
| C10 | math, MEDIUM | `n_min_cell` 40 → 90 (project precedent `mb_current_rung_edge_study.py:172`) |
| C11 | math, MEDIUM | Settlement truth = FINAL available at venue settlement time, not highest revision |
| C12 | architect + codex | Kill-script floor of 15 station-days may be unreachable on 7 tape dates → pre-count; <15 = INSUFFICIENT DATA |
| C13 | architect, MEDIUM | L-16 outer try/except on timer callbacks |
| C14 | architect, MEDIUM | Synthesized obs receipt and per-tick cost are validity caveats |
| C15 | codex, CRITICAL | §10 not machine-executable against the real tree → §13 build contract |
| C16 | housekeeping | Kelly with unified C; density-table provenance |

Verified-correct by the math reviewer (kept): §4 call contract equals P − VWAP − fee(VWAP) − max(floor, VWAP − TOB) against `costs.py:261-342` / `risk.py:732-751`; fee evaluated at VWAP; Kelly odds; ttr sign; as-of forecast gating.

Contradiction resolved by the coordinator: codex said the slippage floor was double-counted, the math reviewer said the algebra was exact but the margin too weak — both true (the floor sat inside `total_prob` AND inside δ); v2 keeps one C and a separate margin.

## Stage 4 — convergence (prediction-market-reviewer verification pass on v2)
C1–C16: all APPLIED (C2 PARTIAL on a factual premise). Verdict NOT CONVERGED on three new findings, all corrected inline in the spec (§14 V1–V3):
- HIGH: §2.4 claimed CRH composes with `SharedExposureView`; verified false (`SharedExposureMixin` is used only by the forecast and lock families; CRH relies on its trial-day latch). Live coexistence therefore needs a live-CRH retrofit — stated as out of scope.
- MEDIUM: §4 \(C(q)\) display used \(p_{exec}\) as the base term (walk counted twice); corrected to \(p_{tob}\). Call contract and §13 test 3 were already right.
- MEDIUM: Xc/X9 were unreachable as refuse reasons if the 0.05 screen ran first; evaluation order pinned (X-rules first) and a test added.
No wrong citations found; the reviewer confirmed `exclusive_conflict` at `risk.py:513-526`, `NwsClimateDay` fields at `nws_climate_day.py:187,211,215`, `n_min=90` precedent, and that `src/breezy/strategy/ladder_ev/` does not collide. Coordinator ruling: CONVERGED after V1–V3 (text-only corrections, no design change).

## Reviewer-named first measurements (all three agree)
Dry, no-submit DEGRADED pass over the converted tape: count afternoon-covered listed
station-days (train window) against the 15-day floor, eligible rows and simulated fills per
station-day, wall-clock and peak RSS per tick, latency arms 1/5/30 s, `n_min` 90 vs 40.
