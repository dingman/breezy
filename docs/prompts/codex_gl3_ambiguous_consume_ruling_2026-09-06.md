You are Codex, acting as the STRATEGY LEAD for Breezy's registered family pm_us_crh_v2 (you authored docs/evidence/codex_prereg_v2_rulings_2026-09-06.md). Read-only: no edits, no git mutations, no network, no process signals. Deliver a RULING, not a code change.
QUESTION (GL-3 in docs/evidence/GO_LIVE_BLOCKERS_2026-09-06.md): the live strategy consumes the one-trial-per-station-day latch on EVERY decision outcome, including `observation_ambiguous` (src/breezy/strategy/current_rung_hold/strategy.py:405-467: `self._latch.consume(...)` runs before the `isinstance(decision, Refuse)` branch; src/breezy/strategy/current_rung_hold/trial_day_latch.py). The registered rule text says an ambiguous instant is not a taken-filter failure: "no legal rung yet → skip, do not consume the station-day. First unambiguous candidate that fails taken does consume the day" (docs/evidence/grok_live_small_spec_rev2_2026-09-04.md:84; PREREG v1 docs/specs/PREREG_v1_current_rung_hold_2026-09-04.md:44 "an ambiguous or non-executable snapshot does not [consume]"). Live effect: MIA and MDW station-days on 09-05 and 09-06 were consumed at their exact window-open minute with reason `observation_ambiguous` (sqlite trial keys; node-log alerts 09-06 17:00:00Z MIA, 18:00:00Z MDW), and no trial could occur those days.
Read the REGISTERED text first: the PREREG v2 spec that manifest `pm_us_crh_v2` (d0=2026-09-05, commit 4975fba) binds — find it under docs/specs/ (the v2 file, its §1/§1b admission rules, §9 covered, §12 screens, §13 binding) and the manifest under deploy/families/; then PREREG v1, spec rev2, the admission-exclusions ack (docs/evidence/grok_admission_exclusions_ack_2026-09-05.md), your own 09-06 rulings, and the strategy/latch/decision code (decision.py rule order for `observation_ambiguous`). Also read tests/unit/test_current_rung_hold_trial_day_latch.py and test_current_rung_hold_decision.py for what the tests currently pin.
RULE on: (1) Does the REGISTERED v2 text (not v1, not rev2 unless v2 incorporates it by reference) specify skip-without-consume for `observation_ambiguous`? Quote the section. (2) Is changing the live code from consume→skip on `observation_ambiguous` a CONFORMANCE fix (the code deviates from the registered rule; fixing it does not change the registered family) or an AMENDMENT (it changes an admission rule and would be a §12 screen requiring re-registration / a new family)? (3) Does the fix contaminate anything already accrued: n=0, the covered-listed denominator (§9, recorder capture only), the structural-dead counter, D0? State plainly. (4) Exactly which refusal reasons must NOT consume (only `observation_ambiguous`? also non-executable / `in_window_no_running_max_yet` / stale observation?) per the registered text — list them with the section for each, and which reasons DO consume (first unambiguous candidate failing the taken filter). (5) Any test that currently pins consume-on-ambiguous and would have to change (name it) — and whether changing it is weakening a contract test or correcting a mis-pinned one. (6) The spec's `n_ambiguous_skip` logging line (rev2:121) — is it registered, and should the fix add that counter?
RETURN (≤ 60 lines): RULING per question with quoted section refs; a one-paragraph BINDING statement suitable for pasting into docs/evidence as the ruling of record; and anything you could not verify.
